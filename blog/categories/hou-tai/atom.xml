<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: 后台 | 干掉这里的Bug]]></title>
  <link href="http://www.wangtianyi.top/blog/categories/hou-tai/atom.xml" rel="self"/>
  <link href="http://www.wangtianyi.top/"/>
  <updated>2021-06-26T14:40:14+08:00</updated>
  <id>http://www.wangtianyi.top/</id>
  <author>
    <name><![CDATA[Tianyi Wang]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[基于Go Micro的微服务架构本地实战]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/03/26/ji-yu-go-microde-wei-fu-wu-jia-gou-ben-di-shi-zhan/"/>
    <updated>2019-03-26T17:00:00+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/03/26/ji-yu-go-microde-wei-fu-wu-jia-gou-ben-di-shi-zhan</id>
    <content type="html"><![CDATA[<p>本项目Github地址：<a href="https://github.com/xbox1994/GoMicroExample">https://github.com/xbox1994/GoMicroExample</a></p>

<!-- more -->


<h2>简介</h2>

<p>Go Micro是一个插件化的基础微服务框架，它是一部分微服务工具的集合。基于该框架可以构建出微服务应用程序。Micro的设计哲学是“可插拔”的插件化架构。在架构之外，它默认实现了consul作为服务发现，通过http、protobuf、进行通信。里面包含几乎所有微服务组件，并且支持非常好的拓展性，通过接口的设计方式，让我们可以拓展一些自己的组件，如服务发现、身份校验、配置管理等。另外，Micro工具包是建立在Go Micro基础之上的微服务工具包，提供更丰富的工具支持。</p>

<p>但即使称自己为框架，但是不是一个完整成熟的微服务体系，所以在下文中会以成熟的微服务体系为目标，在Go Micro的基础上添砖加瓦，以一个简单的例子为例搭建出一套相对完整的微服务架构。</p>

<h2>Go Micro使用</h2>

<p><a href="https://github.com/micro/go-micro">https://github.com/micro/go-micro</a></p>

<p>根据Getting started部分的教程，结合consul、gRPC可以搭建起一个服务之间调用的Demo，这里就不多说了。但是这个Demo和我们想要的微服务架构还是差太多。常见的微服务架构如下图：</p>

<p><img src="/images/blog/2018-09-28_1.jpg" title="&lsquo;image&rsquo;" ></p>

<p>现在我们仅仅实现了Service Discovery以及两个srv之间的交互。下面将一步一步将微服务的主要组件结合Go Micro的<a href="https://micro.mu/docs/go-micro-architecture_cn.html#%E7%9B%B8%E5%85%B3%E5%8C%85">核心组件</a>（注册、选择器、传输、代理、编码、服务端、客户端）加入进来，同时包含一些新手的使用经验。</p>

<h2>服务发现注册</h2>

<h4>原理</h4>

<p>服务注册发现是将所有服务注册到注册中心从而注册中心能提供一个可用的服务列表，让各个服务之间知道彼此的状态与地址。比如客户端可以向注册中心发起查询，来获取服务的位置。下面均以consul为例，说明原理。</p>

<p><img src="/images/blog/2018-09-28_2.jpg" title="&lsquo;image&rsquo;" ></p>

<ul>
<li>当User Service启动的时候，会向consul发送一个POST请求，告诉consul自己的IP和Port</li>
<li>consul 接收到User Service的注册后，每隔10s（默认）会向User Service发送一个健康检查的请求，检验User Service是否健康</li>
<li>当Order Service发送 GET 方式请求/api/addresses到User Service时，会先从consul中拿到一个存储服务 IP 和 Port 的临时表，从表中拿到User Service的IP和Port后再发送GET方式请求/api/addresses</li>
<li>该临时表每隔10s会更新，只包含有通过了健康检查的Service</li>
</ul>


<p>每当发生被注册过的服务之间的调用时，会先从consul中查询可用的IP与Port，才能定位被调用的服务地址。对于已经注册过的服务，应该让consul客户端每隔一段时间会向服务发送一个Health Check的请求来检验服务是否正常。</p>

<p>在Go Micro中，通过在启动服务时执行自身在服务发现中的名称，就能在启动服务时自动注册到consul中：</p>

<pre><code class="go">greeterService := micro.NewService(micro.Name("go.micro.api.greeter"))
greeterService.Init()
if err := greeterService.Run(); err != nil {
    log.Fatal(err)
}
</code></pre>

<h4>健康检查机制带来的问题（Go Micro组件 - 注册 Registry）</h4>

<p>在Go Micro中可以使用consul、etcd、zookeeper、dns、gossip等等提供支持。服务使用启动注册关机卸载的方式注册。服务可以选择性提供过期TTL和定时重注册来保证服务在线，以及在服务不在线时把它清理掉。默认使用的注册中心是mdns，可以使用环境变量<code>MICRO_REGISTRY</code>或命令行<code>--registry</code>指定为consul或其他注册中心。</p>

<p>使用consul之后的坑在于，在程序关闭的时候，如通过IDEA的调试模式启动的程序，或在K8S容器中运行的程序，在退出的时候可能不会发送Registering node的请求，导致consul依然认为该服务正常，依然会把已经关闭的节点当成健康服务，这是go micro中使用consul的<a href="https://www.consul.io/docs/agent/checks.html">健康检查机制</a>的问题。</p>

<p>在Go Micro源码的micro/go-micro/registry/consul_registry.go中，如果不使用TCP或TTL注册方式，则不会告知consul，我已经配置了一个健康检查的机制，所以除非我自己手动解除或在consul里发送命令删除，该节点一直会存在。</p>

<pre><code class="go">var check *consul.AgentServiceCheck

if regTCPCheck {
    deregTTL := getDeregisterTTL(regInterval)

    check = &amp;consul.AgentServiceCheck{
        TCP:                            fmt.Sprintf("%s:%d", node.Address, node.Port),
        Interval:                       fmt.Sprintf("%v", regInterval),
        DeregisterCriticalServiceAfter: fmt.Sprintf("%v", deregTTL),
    }

    // if the TTL is greater than 0 create an associated check
} else if options.TTL &gt; time.Duration(0) {
    deregTTL := getDeregisterTTL(options.TTL)

    check = &amp;consul.AgentServiceCheck{
        TTL:                            fmt.Sprintf("%v", options.TTL),
        DeregisterCriticalServiceAfter: fmt.Sprintf("%v", deregTTL),
    }
}
</code></pre>

<p>所以我们必须添加额外的配置比如<code>MICRO_REGISTER_TTL=10;MICRO_REGISTER_INTERVAL=5;</code>让我们使用TTL的方式，如果10秒内我的服务没有向consul发送请求，服务则是断开状态。也可以使用TCP方式让consul来请求你的服务检查是否健康</p>

<p>但这种方式并不是完美的，如果使用上面的参数，在服务关闭的5秒后，consul中该服务依然是健康的，其他服务请求consul会拿到这个已经关闭的服务的地址。由于consul和etcd都没有和zookeeper的一样的临时节点，无法做到立马在服务关闭的时候让注册中心感知，所以在实时性要求高的服务上下线的情景下应该使用zookeeper</p>

<p>另一方面，上面我们写了三种健康检查的具体方式，可以大体分为客户端心跳（TTL、临时节点）和服务端主动探测（TCP）两种方式。客户端心跳中，长连接的维持和客户端的主动心跳都只是表明链路上的正常，不一定是服务状态正常。服务端主动调用服务进行健康检查是一个较为准确的方式，返回结果成功表明服务状态确实正常，但也存在问题。服务注册中心主动调用 RPC 服务的某个接口无法做到通用性；在很多场景下服务注册中心到服务发布者的网络是不通的，服务端无法主动发起健康检查。所以如何取舍，还是需要根据实际情况来决定，根据不同的场景，选择不同的策略。</p>

<h4>服务之间的负载均衡（Go Micro组件 - 选择器 Selector）</h4>

<p>选择器是构建在服务注册中心上的负载均衡抽象，负责通过Go Micro组件 - 客户端调用内部服务的策略。它允许服务被过滤函数过滤掉不提供服务，也可以通过选择适当的算法来被选中提供服务，算法可以是随机、轮询（客户端均衡）、最少链接（leastconn）等等。选择器通过客户端创建语法时发生作用。客户端会使用选择器而不是注册表，因为它提供内置的负载均衡机制。</p>

<p>consul提供的负载均衡能力太弱，只有作为DNS查询的时候可以使用随机循环策略进行负载均衡，所以一般是<a href="https://www.hashicorp.com/blog/load-balancing-strategies-for-consul">结合Fabio、Nginx</a>进行真实有效的负载均衡。在Go Micro中，请求路由访问某个服务的过程是（以consul为例）：</p>

<ol>
<li>根据服务名称去查询consul得到所有该服务的节点列表（micro/go-micro/registry/consul_registry.go GetService方法）</li>
<li>根据选择器策略从节点列表中选择一个节点并发送请求（micro/go-micro/client/rpc_client.go next方法）</li>
</ol>


<p>默认使用的是<code>micro/go-micro/selector/strategy.go</code>中的Random算法随机选择节点，也可以自行定义算法选择节点，可以参考<a href="https://github.com/micro/examples/blob/master/client/selector/selector.go">这里</a>实现自己的selector，本项目中不包含选择器的实现</p>

<h2>API 网关</h2>

<p>API Gateway 是随着微服务概念一起兴起的一种架构模式，它用于解决微服务过于分散，没有一个统一的出入口进行流量管理的问题。它类似于面向对象的外观模式。流量管理的具体问题包括：封装了内部系统架构并且提供了每个客户端定制的API、身份验证、监控、负载均衡、缓存、数据通信协议转换、统一维护流量路由表。</p>

<p><img src="/images/blog/2018-09-28_3.jpg" title="&lsquo;image&rsquo;" ></p>

<p>API Gateway的缺点是，它必须是一个被开发、部署、管理的高可用组件。另外，开发人员必须更新API网关来暴露每个微服务接口。因此更新API网关的过程越少越好。然而，对于大多数现实世界的应用，使用API网关是必不可少的。</p>

<h4>Micro Api</h4>

<p>Micro Api是Micro工具包中实现API网关的工具，它提供HTTP并使用服务发现动态路由到适当的后端。API的请求通过HTTP提供，并通过RPC进行内部路由。我们可以利用它进行服务发现，负载平衡，编码和基于RPC的通信。</p>

<p><img src="/images/blog/2018-09-28_4.png" title="&lsquo;image&rsquo;" ></p>

<p>上图是官方给的Micro Api路由图，Micro Api是一个请求路由、格式转换的工具，只需要执行<code>micro api</code>来启动它，就可以像其他服务一样自动注册到consul，在<code>8080</code>端口开启所有外界HTTP请求的访问入口，并将请求路由、转换到内部中。至于向加一些其他功能比如鉴权则需要实现Micro Api的插件，可以参考项目中的实现。</p>

<h4>数据格式转换（Go Micro组件 - 编码 Codec）</h4>

<p>编码组件用于在消息传输到两端时进行编码与解码，可以是json、protobuf、bson、msgpack等等。与其它编码方式不同的我们支持RPC格式的。所以我们有JSON-RPC、PROTO-RPC、BSON-RPC等格式。编码包把客户端与服务端的编码隔离开来，并提供强大的方法来集成其它系统，比如gRPC、Vanadium等等。</p>

<p>编码组件是框架内部的工作，开发时不需要关心，我们需要关心的是：前端需要使用Json来进行通信，但后端使用Protobuf在服务之间通信。对于外部请求，Micro Api通过接收前端HTTP请求，将请求转换之后发送到内部服务，对于服务间的请求，A服务可以直接使用go语言的gRPC客户端调用B服务的接口。相比在使用Restful方式完成服务之间的相互访问，gRPC能提供更好的性能，更低的延迟，并且生来适合与分布式系统。</p>

<p>gRPC是一个高性能、通用的开源RPC框架，其由Google主要面向移动应用开发并基于HTTP/2协议标准而设计，同时基于标准化的IDL（ProtoBuf）来生成服务器端和客户端代码, ProtoBuf服务定义可以作为服务契约，因此可以更好的支持团队与团队之间的接口设计，开发，测试，协作。</p>

<p>protoc-gen-micro是与Micro工具包类似的一个工具，在gRPC的基础之上封装了一层更加规范和易于使用的接口，我们只需要写出proto文件，执行<code>protoc --proto_path=proto --proto_path=${GOPATH} --micro_out=proto --go_out=proto greeter.proto</code>就能生成go语言版本接口文件，接口文件中定义了服务提供者应该实现哪些接口，服务调用者应该如何使用的接口。现在就需要实现服务端的接口之后在启动服务的代码中注册该proto对应的接口，如下：</p>

<p>proto文件：
<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
</pre></td><td class='code'><pre><code class='proto'><span class='line'><span class="na">syntax</span> <span class="o">=</span> <span class="err">&amp;</span><span class="n">ldquo</span><span class="p">;</span><span class="n">proto3</span><span class="err">&amp;</span><span class="n">rdquo</span><span class="p">;;</span><span class="o">&lt;/</span><span class="n">p</span><span class="err">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="err">&gt;</span><span class="k">import</span> <span class="err">&amp;</span><span class="n">ldquo</span><span class="p">;</span><span class="n">github.com</span><span class="o">/</span><span class="n">micro</span><span class="o">/</span><span class="n">go</span><span class="o">-</span><span class="n">api</span><span class="o">/</span><span class="n">proto</span><span class="o">/</span><span class="n">api.proto</span><span class="err">&amp;</span><span class="n">rdquo</span><span class="p">;;</span><span class="o">&lt;/</span><span class="n">p</span><span class="err">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="err">&gt;</span><span class="kd">service</span> <span class="n">Greeter</span> <span class="p">{</span>
</span><span class='line'>    <span class="k">rpc</span> <span class="n">Hello</span><span class="p">(</span><span class="n">go.api.Request</span><span class="p">)</span> <span class="k">returns</span><span class="p">(</span><span class="n">go.api.Response</span><span class="p">)</span> <span class="p">{};</span>  <span class="c1">//对外的服务接口必须使用go-api的request与response，因为是与Micro Api结合使用需要统一接口</span>
</span><span class='line'>                                                            <span class="c1">//对内的服务可以自定义参数与返回，可参考service/user/proto/user.proto</span>
</span><span class='line'><span class="p">}</span>
</span></code></pre></td></tr></table></div></figure></p>

<h4>统一流量入口，维护路由关系</h4>

<p>Micro Api根据HTTP请求路径、内部服务名称、被调用方法名建立了如下的映射关系，将所有前端传递过来的请求根据该表路由到后端服务中，可以避免从前端直接调用多个后端服务而产生的混乱的交互逻辑，也相当于避免暴露出后端服务。</p>

<table>
<thead>
<tr>
<th>Path</th>
<th>Service</th>
<th>Method</th>
</tr>
</thead>
<tbody>
<tr>
<td>/foo/bar    </td>
<td>   go.micro.api.foo    </td>
<td>   Foo.Bar</td>
</tr>
<tr>
<td>/foo/bar/baz    </td>
<td>   go.micro.api.foo    </td>
<td>   Bar.Baz</td>
</tr>
<tr>
<td>/foo/bar/baz/cat    </td>
<td>   go.micro.api.foo.bar    </td>
<td>   Baz.Cat</td>
</tr>
</tbody>
</table>


<p>所以我们在写服务的时候需要注意服务的名称与方法名正确命名，比如<code>http://localhost:8080/greeter/hello</code>会映射到如下方法：</p>

<pre><code class="go">type Greeter struct {
    userClient user.UserService
}

func (ga *Greeter) Hello(ctx context.Context, req *go_api.Request, rsp *go_api.Response) error {
}
</code></pre>

<h4>用户身份鉴定</h4>

<p>该功能是除了登录功能的每一个API都会调用的功能，甚至为了不嵌入到每一个服务中，统一在这里处理较好。对于容器化与无状态服务，可以使用JWT进行身份验证，以下是一个例子，从Authorization中解析出用户信息然后传递给网关后的服务。</p>

<pre><code class="go">func (*Auth) Handler() plugin.Handler {
    return func(h http.Handler) http.Handler {
        return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
            log.Println("auth plugin received: " + r.URL.Path)
            if r.URL.Path == "/user/login" {
                h.ServeHTTP(w, r)
                return
            }

            token := r.Header.Get("Authorization")
            userFromToken, e := Decode(token)

            if e != nil {
                response, _ := json.Marshal(util.CommonResponse{
                    Code:    code.AuthorizationError,
                    Message: "please login",
                })
                w.Write(response)
                return
            }

            r.Header.Set("X-Example-Id", userFromToken.Id)
            r.Header.Set("X-Example-Username", userFromToken.Username)
            h.ServeHTTP(w, r)
            return
        })
    }
}
</code></pre>

<h2>服务端（Go Micro组件 - 服务端 Server）</h2>

<p>Server包是使用编写服务的构建包，可以命名服务，注册请求处理器，增加中间件等等。服务构建在以上说的包之上，提供独立的接口来服务请求。现在服务的构建是RPC系统，在未来可能还会有其它的实现。服务端允许定义多个不同的编码来服务不同的编码消息。</p>

<p>比如Greeter的main.go文件，只需要实现proto文件中的Hello接口，就可以被当做服务端，结合Go Micro服务端组件使用，成为一个可被访问的服务：</p>

<pre><code class="go">type Greeter struct {
    userClient user.UserService
}

func (ga *Greeter) Hello(ctx context.Context, req *go_api.Request, rsp *go_api.Response) error {
    log.Print("Received Greeter.Hello API request")
    return nil
}

func main() {
    greeterService := micro.NewService(
        micro.Name("go.micro.api.greeter"),
    )
    greeterService.Init()
    greeterApi.RegisterGreeterHandler(greeterService.Server(), &amp;Greeter{
        userClient: user.NewUserService("go.micro.api.user", greeterService.Client())})

    if err := greeterService.Run(); err != nil {
        log.Fatal(err)
    }
}
</code></pre>

<h2>客户端（Go Micro组件 - 客户端 Client）</h2>

<p>客户端提供接口来创建向服务端的请求。与服务端类似，它构建在其它包之上，它提供独立的接口，通过注册中心来基于名称发现服务，基于选择器（selector）来负载均衡，使用transport、broker处理同步、异步消息。</p>

<p>比如greeter服务想要调用user服务，那么就需要将user服务的proto接口文件放到greeter服务能访问到的地方，让greeter的代码能够与之共同构建，还需要在创建greeter服务的时候将user服务的客户端创建出来，如下：</p>

<pre><code class="go">func main() {
    greeterService := micro.NewService(micro.Name(micro_c.MicroNameGreeter),)
    greeterService.Init()
    greeterApi.RegisterGreeterHandler(greeterService.Server(), &amp;Greeter{
        userClient: user.NewUserService(micro_c.MicroNameUser, greeterService.Client())})
    if err := greeterService.Run(); err != nil {
        log.Fatal(err)
    }
}
</code></pre>

<h2>服务间异步通信（Go Micro组件 - 代理 Broker）</h2>

<p>Broker提供异步通信的消息发布/订阅接口。对于微服务系统及事件驱动型的架构来说，发布/订阅是基础。一开始，默认我们使用收件箱方式的点到点HTTP系统来最小化依赖的数量。但是，在go-plugins是提供有消息代理实现的，比如RabbitMQ、NATS、NSQ、Google Cloud Pub Sub等等。</p>

<p>具体使用方法参看：<a href="https://github.com/micro/examples/tree/master/broker">https://github.com/micro/examples/tree/master/broker</a></p>

<h2>服务配置中心</h2>

<p>最开始，我们的每个服务的配置文件都是在自身代码库中，当服务数量、服务所在的环境的数量达到一定数量后，管理这些分散的配置文件会成为一个痛点。由于Micro Config不支持从Git上获取配置文件，所以选用Spring Cloud Config来实现。</p>

<p>Spring Cloud Config的目标是将各个微服务的配置文件集中存储一个文件仓库中（比如系统目录，Git仓库等等），然后通过Config Server从文件仓库中去读取配置文件，而各个微服务作为Viper通过给Config Server发送请求来获取特定的Profile的配置文件，从而为自身的应用提供配置信息。</p>

<p><img src="/images/blog/2018-09-28_5.jpg" title="&lsquo;image&rsquo;" ></p>

<p>具体配置过程参看：<a href="https://www.jianshu.com/p/e60f3cceaace">https://www.jianshu.com/p/e60f3cceaace</a> 以及本项目中的<code>service/config/config.go</code>文件、<code>config</code>项目。</p>

<h2>服务容错保护</h2>

<p>服务熔断：当服务A调用服务B的请求满足一定的规则，比如10秒内请求数达到20个，并且有一半以上的请求失败了，此时我们通过切断对服务B的调用来保护系统的整体响应，这种操作即为服务熔断。<br/>
服务降级：在服务B被熔断之后，服务A不会真正地调用服务B。取而代之的是，我们在服务A中定义一个服务降级逻辑（通常是一个fallback接口），此时服务A会直接调用服务降级逻辑快速获取返回结果。</p>

<p>下面是hystrix的配置文件，定义了服务熔断标准、服务降级逻辑、数据暴露端口</p>

<pre><code class="go">type clientWrapper struct {
    client.Client
}

func (c *clientWrapper) Call(ctx context.Context, req client.Request, rsp interface{}, opts ...client.CallOption) error {
    return hystrix.Do(req.Service()+"."+req.Method(), func() error {
        return c.Client.Call(ctx, req, rsp, opts...)
    }, func(err error) error {
        // 可以在这里自定义服务降级逻辑
        log.Printf("fallback error!!!!!  %v", err)
        return err
    })
}

func NewClientWrapper() client.Wrapper {
    return func(c client.Client) client.Client {
        return &amp;clientWrapper{c}
    }
}

func Configure(names []string) {
    // hystrix有默认的参数配置，这里可以对某些api进行自定义配置
    config := hystrix.CommandConfig{
        Timeout:               2000,
        MaxConcurrentRequests: 100,
        ErrorPercentThreshold: 25,
    }
    configs := make(map[string]hystrix.CommandConfig)
    for _, name := range names {
        configs[name] = config
    }
    hystrix.Configure(configs)

    // 还能结合hystrix dashboard来将服务状态动态可视化出来
    hystrixStreamHandler := hystrix.NewStreamHandler()
    hystrixStreamHandler.Start()
    go http.ListenAndServe(net.JoinHostPort("", "8181"), hystrixStreamHandler)
    log.Println("Launched hystrixStreamHandler at 8181")
}
</code></pre>

<p>在服务初始化的时候只需要如下配置就能利用以上的配置来针对某些接口进行容错保护，一旦到达服务降级阈值，则会调用到降级逻辑</p>

<pre><code class="go">hystrix.Configure([]string{"go.micro.api.user.User.GetUserInfo"})
greeterService := micro.NewService(
    micro.Name(micro_c.MicroNameGreeter),
    micro.WrapClient(hystrix.NewClientWrapper()),
)
</code></pre>

<h2>整体架构</h2>

<p>以 greeter/hello 为例，请求流程图如下：</p>

<p><img src="/images/blog/2018-09-28_6.jpg" title="&lsquo;image&rsquo;" ></p>

<ol>
<li>访问greeter/hello</li>
<li>Micro Api解析请求，验证身份是否有效，决定请求是否继续传递，如果有效，则将解析出来的用户信息写到header中</li>
<li>GreeterService接收到gRPC请求并路由到Greeter.Hello方法中，调用其中的逻辑，尝试发送请求到UserService得到用户的其他信息，header也转发过去</li>
<li>UserService根据header中得到的id查询数据库得到具体的用户信息并返回</li>
</ol>


<p>在服务间请求调用过程中，会有Hystrix来提供服务容错机制。在所有服务启动之前，会请求Config Service来获得对应服务的对应环境的配置信息。</p>

<h2>参考</h2>

<p><a href="https://gocn.vip/question/1999">https://gocn.vip/question/1999</a><br/>
<a href="http://sjyuan.cc/topics/">http://sjyuan.cc/topics/</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[OAuth2原理与LinkedIn的第三方分享实战]]></title>
    <link href="http://www.wangtianyi.top/blog/2018/12/30/oauth2yuan-li-yu-linkedinde-di-san-fang-fen-xiang-shi-zhan/"/>
    <updated>2018-12-30T23:19:45+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2018/12/30/oauth2yuan-li-yu-linkedinde-di-san-fang-fen-xiang-shi-zhan</id>
    <content type="html"><![CDATA[<!-- more -->


<h2>OAuth是什么</h2>

<p>开放授权（OAuth）是一个开放标准，允许用户让第三方应用访问该用户在某一网站上存储的私密的资源（如照片，视频，联系人列表），而无需将用户名和密码提供给第三方应用。在全世界得到广泛应用，目前的版本是2.0版。</p>

<p>比如我的应用需要实现在领英上替用户分享一个动态，但是只有得到用户的授权才能在我的应用中调用领英的API进行分享操作，如果直接让用户把用户名密码传到我的应用，确实可以实现，但是有以下问题：</p>

<ul>
<li>为了后续的服务，我的应用会保存用户的密码，在我的应用中保存密码很不安全</li>
<li>用户无法限制我的应用中能进行哪些操作</li>
<li>领英必须要有密码登录的功能，而单纯的密码登录并不安全</li>
<li>我的应用中还需要处理验证码这样的额外身份验证情况</li>
<li>用户只有修改密码，才能收回权限，但会让所有获得用户授权的第三方应用程序全部失效</li>
<li>只要有一个第三方应用程序被破解，就会导致用户密码泄漏，以及所有被密码保护的数据泄漏</li>
</ul>


<p>OAuth就是为了解决上面这些问题而诞生的。</p>

<h2>OAuth原理</h2>

<pre><code> +--------+                               +---------------+
 |        |--(A)- Authorization Request -&gt;|   Resource    |
 |        |                               |     Owner     |
 |        |&lt;-(B)-- Authorization Grant ---|               |
 |        |                               +---------------+
 |        |
 |        |                               +---------------+
 |        |--(C)-- Authorization Grant --&gt;| Authorization |
 | Client |                               |     Server    |
 |        |&lt;-(D)----- Access Token -------|               |
 |        |                               +---------------+
 |        |
 |        |                               +---------------+
 |        |--(E)----- Access Token ------&gt;|    Resource   |
 |        |                               |     Server    |
 |        |&lt;-(F)--- Protected Resource ---|               |
 +--------+                               +---------------+
</code></pre>

<ul>
<li>Client：客户端，第三方应用，我的应用就是一个Client</li>
<li>Resource Owner ：用户</li>
<li>Authorization Server：授权服务器，即提供第三方登录服务的服务器，如领英</li>
<li>Resource Server：服务提供商，通常和授权服务器属于同一应用，如领英</li>
</ul>


<p>根据上图的信息，我们可以知道OAuth2的基本流程为：
1. 用户调用第三方应用的功能，如分享，该功能需要使用用户在服务提供商上的权限，所以第三方应用直接重定向到授权服务器的登录、授权页面等待用户登录、授权。
2. 用户登录之后同意授权，并返回一个凭证（code）给第三方应用
3. 第三方应用通过第二步的凭证（code）向授权服务器请求授权
4. 授权服务器验证凭证（code）通过后，同意授权，并返回一个服务访问的凭证（Access Token）。
5. 第三方应用通过第四步的凭证（Access Token）向服务提供商请求相关资源。
6. 服务提供商验证凭证（Access Token）通过后，将第三方应用请求的资源返回。</p>

<p>简单来说，OAuth在"客户端"与"服务提供商"之间，设置了一个授权层。"客户端"不能直接登录"服务提供商"，只能登录"授权层"，以此将用户与客户端区分开来。"客户端"登录"授权层"所用的凭证（Access Token），与用户的密码不同。用户可以在登录的时候，授权服务器指定授权层令牌的权限范围和有效期。</p>

<h2>LinkedIn的OAuth2交互</h2>

<p>下面以在我的应用上实现在领英中替用户分享一个动态为例子，真切地来体验一把OAuth2。</p>

<p>交互图如下，细节会在下面写到：</p>

<p><img src="/images/blog/2018-12-30_1.png" title="&lsquo;image&rsquo;" ></p>

<h3>配置LinkedIn应用程序</h3>

<p>除了领英，其他任意开发者平台都需要我们在平台上注册一个App才能给我们调用平台API的权限，也方便平台对我们的资质审核以及调用管理。</p>

<p>所以首先我们得去领英开发者平台去注册一个App：
<a href="https://www.linkedin.com/developer/apps/new">https://www.linkedin.com/developer/apps/new</a></p>

<p><img src="/images/blog/2018-12-30_2.png" title="&lsquo;image&rsquo;" ></p>

<ul>
<li>Keys: 完成之后会得到App相关的Key，这是在后续OAuth2交互时需要向领英提供身份验证。</li>
<li>权限：在用户登录时会给用户展示出我的应用需要哪些权限，我的应用最后调用API的时候会检测权限是否允许。</li>
<li>回调API接口：这是在OAuth2交互时需要验证的一个参数，领英只会与已识别为可信终端的URL进行通信。</li>
</ul>


<h3>获取用户授权凭证Code</h3>

<p>用户在我的应用中点击【分享】按钮，会发送一个GET请求到<code>myApp/linkedin/auth/authorization</code>，其中<code>linkedin/auth/authorization</code>是我的应用里暴露出的一个专门用来进行验证的接口。</p>

<p>我的应用收到请求之后直接回复重定向到领英的授权页面，并且重定向的url中必须包含领英实现的OAuth2的一些参数，如下：
<img src="/images/blog/2018-12-30_3.png" title="&lsquo;image&rsquo;" ></p>

<p>其中state参数是防止csrf攻击加入进来的，关于csrf以及本例中对csrf防范的实现会在文章最后一部分提到。</p>

<p>在go语言+Beego框架中的实现如下：</p>

<pre><code class="go">func (c *AuthorizationController) Get() {
    host := beego.AppConfig.String("host")
    state := util.Generate(10) // random string with length 10
    clientId := beego.AppConfig.String("linkedin_client_id") // 创建app得到的key
    linkedinOauth2AuthorizationUrl := "https://www.linkedin.com/oauth/v2/authorization?response_type=code&amp;client_id=%s&amp;redirect_uri=%s/linkedin/auth/callback&amp;state=%s"
    uri := fmt.Sprintf(linkedinOauth2AuthorizationUrl, clientId, host, state)

    c.SetSession("_csrf_Token", state)
    c.Redirect(uri, 302)
}
</code></pre>

<h3>浏览器跳转到领英页面等待授权</h3>

<p>浏览器从上一步中根据重定向url跳转到领英页面，等待用户登录并授权，如果用户已经登录就直接跳转，如下图：</p>

<p><img src="/images/blog/2018-12-30_4.jpg" title="&lsquo;image&rsquo;" ></p>

<p>用户如果点击Cancel，或者请求因任何其他原因而失败，则会将其重定向回redirect_uri的URL，并附加一些错误参数。</p>

<p>用户如果点击Allow，用户批准您的应用程序访问其成员数据并代表他们与LinkedIn进行交互，也会将其重定向回redirect_uri，并附加重要参数<strong>code</strong></p>

<h3>通过Code拿到Token，并实现分享</h3>

<p>redirect_uri就是我的应用中的callback接口，这个接口等待用户传递一个允许授权的凭证code，在我的应用中就可以拿这个code去领英中申请access token，以后就拿着这个access token去访问领英中允许访问的资源了</p>

<pre><code class="go">func (c *CallbackController) Get() {
    // csrf validation
    csrfToken := c.GetSession("_csrf_Token")
    if csrfToken != c.GetString("state") {
        c.Data["json"] = map[string]interface{}{
            "error":             c.GetString("csrf error"),
            "error_description": c.GetString("error_description")}
        c.ServeJSON()
        return
    }

    // user cancel authorization request or linkedin server error
    if c.GetString("error") != "" {
        c.Data["json"] = map[string]interface{}{
            "error":             c.GetString("error"),
            "error_description": c.GetString("error_description")}
        c.ServeJSON()
        return
    }

    // user accept authorization request
    code := c.GetString("code")
    if code != "" {
        // get access token by code
        accessToken := getAccessToken(code)
        // share by access token
        shareResponse := share(accessToken.AccessToken)
        c.Data["json"] = &amp;shareResponse
        c.ServeJSON()
        return
    }
}
</code></pre>

<p>getAccessToken和share方法仅仅是发送请求到领英的REST API接口，实际上如果领英有golang的sdk的话，就不需要我们自己使用golang的http包来自己封装请求处理结果了，但可惜领英并没有。</p>

<p>最后给一个GIF图，该图包含了所有的流程展示，从点击【分享】按钮（调用localhost/linkedin/auth/authorization接口）开始：</p>

<p><img src="/images/blog/2018-12-30_5.gif" title="&lsquo;image&rsquo;" ></p>

<h2>CSRF</h2>

<p><a href="http://en.wikipedia.org/wiki/Cross-site_request_forgery">跨站请求伪造(Cross-site request forgery)</a>， 简称为 CSRF，是 Web 应用中常见的一个安全问题。前面的链接也详细讲述了 CSRF 攻击的实现方式。</p>

<p>当前防范 CSRF 的一种通用的方法，是对每一个用户都记录一个无法预知的 cookie 数据，然后要求所有提交的请求（POST/PUT/DELETE）中都必须带有这个 cookie 数据。如果此数据不匹配 ，那么这个请求就可能是被伪造的。</p>

<p>我们这里防范CSRF的方法就是通过在用户第一次点击分享时（调用/linkedin/auth/authorization）在Session中存储一个字符串：</p>

<pre><code class="go">func (c *AuthorizationController) Get() {
    ...
    c.SetSession("_csrf_Token", state)
    ...
}
</code></pre>

<p>当用户浏览器拿到用户的授权凭证code之后发送给我的应用时（调用/linkedin/auth/callback）检测此时请求中的state与之前在Session中存储的字符串是否相同。</p>

<pre><code class="go">
func (c *CallbackController) Get() {
    ...
    csrfToken := c.GetSession("_csrf_Token")
    if csrfToken != c.GetString("state") {
        c.Data["json"] = map[string]interface{}{"error": "xsrf error"}
        c.ServeJSON()
        return
    }
    ...
}
</code></pre>

<p>如果相同则能判断是用户的请求，如果不同，则可能是用户点击了其他用户伪造的一个链接发送的请求，从而可以防止code被发送到恶意网站上去</p>

<h2>本文资源</h2>

<p>代码及部分图片工程文件：<a href="https://github.com/xbox1994/OAuth2-LinkedIn.git">https://github.com/xbox1994/OAuth2-LinkedIn.git</a></p>

<h2>参考</h2>

<p><a href="https://zh.wikipedia.org/wiki/%E5%BC%80%E6%94%BE%E6%8E%88%E6%9D%83">https://zh.wikipedia.org/wiki/%E5%BC%80%E6%94%BE%E6%8E%88%E6%9D%83</a><br/>
<a href="http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html">http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html</a><br/>
<a href="https://developer.linkedin.com/docs/share-on-linkedin">https://developer.linkedin.com/docs/share-on-linkedin</a><br/>
<a href="https://zhuanlan.zhihu.com/p/20913727">https://zhuanlan.zhihu.com/p/20913727</a><br/>
<a href="https://www.cnblogs.com/flashsun/p/7424071.html">https://www.cnblogs.com/flashsun/p/7424071.html</a><br/>
<a href="https://beego.me/docs/mvc/controller/xsrf.md">https://beego.me/docs/mvc/controller/xsrf.md</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[一次简单通用的压测调优实战]]></title>
    <link href="http://www.wangtianyi.top/blog/2018/12/08/yi-ci-ji-yu-wei-fu-wu-kuang-jia-de-tong-yong-ya-ce-diao-you-shi-zhan/"/>
    <updated>2018-12-08T20:16:55+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2018/12/08/yi-ci-ji-yu-wei-fu-wu-kuang-jia-de-tong-yong-ya-ce-diao-you-shi-zhan</id>
    <content type="html"><![CDATA[<p>相比于网上教你如何使用ab、jmeter工具进行压测一个接口，本文更关注在压测调优的思路上。</p>

<!-- more -->


<h2>明确思路</h2>

<p>目的：提升单个接口的QPS（每秒查询率QPS是对一个特定的查询服务器在规定时间内所处理流量多少的衡量标准。）</p>

<p>工具：本文不关注工具的使用，简单起见，使用apache的<code>ab</code>工具</p>

<p>步骤：先测试并优化一个空接口（直接返回请求的接口），拿到一个极限QPS，然后分别引进Redis、MySQL的接口分别进行测试并优化，使其尽量接近极限QPS。步骤严格遵循控制变量法。</p>

<p>环境：为得到尽量接近真实生产环境的数据，需要提前建立一个与生产环境几乎一致的压测环境，在压测环境上进行测试。当前环境为：AWS EC2上部署的K8S集群，共2个Node节点，CPU为2核。</p>

<h2>空接口压测优化</h2>

<h3>获取数据</h3>

<p>先在代码中写上一个直接返回请求的test接口，然后部署到容器集群中作为被压测的对象，再登录到另外一个作为施压机进行压测。</p>

<p>同时注意检测CPU，内存，网络状态，因为对于一个空接口，QPS达到上限一定是因为该服务所在宿主机的某处性能达到瓶颈。</p>

<p>命令：<code>ab -n 3000 -c 300 "http://10.1.2.3/test/"</code></p>

<p>测试环境：
<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
</pre></td><td class='code'><pre><code class='proto'><span class='line'><span class="n">Requests</span> <span class="n">per</span> <span class="n">second</span><span class="o">:</span>    <span class="mf">1448.86</span> <span class="p">[</span><span class="err">#</span><span class="o">/</span><span class="n">sec</span><span class="p">]</span> <span class="p">(</span><span class="n">mean</span><span class="p">)</span>
</span><span class='line'><span class="n">Time</span> <span class="n">per</span> <span class="n">request</span><span class="o">:</span>       <span class="mf">69.020</span> <span class="p">[</span><span class="n">ms</span><span class="p">]</span> <span class="p">(</span><span class="n">mean</span><span class="p">)</span><span class="o">&lt;/</span><span class="n">p</span><span class="err">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="err">&gt;</span><span class="n">Connection</span> <span class="n">Times</span> <span class="p">(</span><span class="n">ms</span><span class="p">)</span>
</span><span class='line'>              <span class="n">min</span>  <span class="n">mean</span><span class="p">[</span><span class="o">+/-</span><span class="n">sd</span><span class="p">]</span> <span class="n">median</span>   <span class="k">max</span>
</span><span class='line'><span class="n">Connect</span><span class="o">:</span>        <span class="mi">1</span>    <span class="mi">1</span>   <span class="mf">1.1</span>      <span class="mi">1</span>       <span class="mi">8</span>
</span><span class='line'><span class="n">Processing</span><span class="o">:</span>     <span class="mi">5</span>   <span class="mi">66</span>  <span class="mf">29.4</span>     <span class="mi">63</span>     <span class="mi">164</span>
</span><span class='line'><span class="n">Waiting</span><span class="o">:</span>        <span class="mi">5</span>   <span class="mi">66</span>  <span class="mf">29.4</span>     <span class="mi">63</span>     <span class="mi">164</span>
</span><span class='line'><span class="n">Total</span><span class="o">:</span>          <span class="mi">5</span>   <span class="mi">67</span>  <span class="mf">29.7</span>     <span class="mi">64</span>     <span class="mi">165</span>
</span></code></pre></td></tr></table></div></figure></p>

<p>在整个压测过程中，使用<code>top -d 0.3</code>发现CPU达到瓶颈，为验证我们的猜想，将相同的服务部署到另外一个CPU性能更好的机器上测试。</p>

<p>CPU性能提升之后的本地环境：
<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
</pre></td><td class='code'><pre><code class='proto'><span class='line'><span class="n">Requests</span> <span class="n">per</span> <span class="n">second</span><span class="o">:</span>    <span class="mf">3395.25</span> <span class="p">[</span><span class="err">#</span><span class="o">/</span><span class="n">sec</span><span class="p">]</span> <span class="p">(</span><span class="n">mean</span><span class="p">)</span>
</span><span class='line'><span class="n">Time</span> <span class="n">per</span> <span class="n">request</span><span class="o">:</span>       <span class="mf">29.453</span> <span class="p">[</span><span class="n">ms</span><span class="p">]</span> <span class="p">(</span><span class="n">mean</span><span class="p">)</span><span class="o">&lt;/</span><span class="n">p</span><span class="err">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="err">&gt;</span><span class="n">Connection</span> <span class="n">Times</span> <span class="p">(</span><span class="n">ms</span><span class="p">)</span>
</span><span class='line'>              <span class="n">min</span>  <span class="n">mean</span><span class="p">[</span><span class="o">+/-</span><span class="n">sd</span><span class="p">]</span> <span class="n">median</span>   <span class="k">max</span>
</span><span class='line'><span class="n">Connect</span><span class="o">:</span>        <span class="mi">0</span>    <span class="mi">0</span>   <span class="mf">0.7</span>      <span class="mi">0</span>       <span class="mi">3</span>
</span><span class='line'><span class="n">Processing</span><span class="o">:</span>     <span class="mi">2</span>   <span class="mi">28</span>  <span class="mf">13.3</span>     <span class="mi">26</span>      <span class="mi">84</span>
</span><span class='line'><span class="n">Waiting</span><span class="o">:</span>        <span class="mi">2</span>   <span class="mi">28</span>  <span class="mf">13.3</span>     <span class="mi">26</span>      <span class="mi">84</span>
</span><span class='line'><span class="n">Total</span><span class="o">:</span>          <span class="mi">2</span>   <span class="mi">29</span>  <span class="mf">13.4</span>     <span class="mi">26</span>      <span class="mi">86</span>
</span></code></pre></td></tr></table></div></figure></p>

<h3>定位问题</h3>

<p>Processing time的解释：The server response time—i.e., the time it took for the server to process the request and send a reply</p>

<p>结论很明显，在Connect连接时长几乎一致的情况下，服务器端处理时间大大减少，所以<strong>CPU</strong>对QPS的影响是最直接的。</p>

<p>另外，依据经验判断，2核CPU的机器本地压测只有3k是不正常的一个数据，所以猜测框架性能有差异，所以对不同框架进行了空接口的测试，数据如下：</p>

<p><code>beego: 12k , go micro: 3.5k, spring boot: 5.5k</code></p>

<p>结论是<strong>框架</strong>直接大幅度影响性能，分析一下，go micro与beego、spring boot的区别是它是一个<strong>微服务</strong>架构的框架，需要consul、api gateway、后台服务一起启动，一个请求来到gateway之后，可能需要查询consul拿到后台服务的地址，还需要将JSON格式转换为gRPC格式发送给后台服务，然后接收后台服务的返回，这直接导致了在CPU计算量、网络传输量最少两倍于单体应用，即使go micro的网关和后台服务分开在两台机器上部署，测试之后QPS也只能到达5.5k。</p>

<p>我是直接感受到了一个微服务的缺点：相比于单体架构服务的直接返回请求，微服务架构服务的开销是很可能更大的，所以在选择架构的时候需要在性能与微服务带来的优势（服务解耦、职责单一、独立开发构建部署测试)上进行衡量，当然如果你服务器够多性能够好，当我没说。</p>

<h3>进行优化</h3>

<p>在CPU无法提升、框架无法改变的情况下，只能在框架和服务的配置、代码的使用、架构层面进行优化。</p>

<h4>1</h4>

<p>参考go micro作者给的<a href="https://micro.mu/docs/faq.html#how-performant-is-it">方法</a>，对框架的配置进行优化：</p>

<p><code>--client_pool_size=10 # enables the client side connection pool</code></p>

<p>再次在测试环境下测试，QPS提升300，到达1700。</p>

<h4>2</h4>

<p>api gateway是所有流量都会走的地方，所以是优化的重要部分，我们先测试一下删掉其中的业务代码，QPS提升了600，到达2300，所以优化了一下这里的逻辑，QPS到达2100</p>

<pre><code class="go">    if !strings.Contains(cookies, "xxx") {
        return nil, errors.New("no xxx cookie found")
    }
</code></pre>

<p>替换掉</p>

<pre><code class="go">    cookiesArr := strings.Split(cookies, ";")
    for _, v := range cookiesArr {
        cookie := strings.Split(v, "=")
        if strings.Trim(cookie[0], " ") == "xxx" {
            sid = cookie[1]
            break
        }
    }
</code></pre>

<h4>3</h4>

<p>最后，使用k8s的自动伸缩机制，另外部署了一组api gateway和后台服务到不同的Node上，相当于配置好了两台机器的负载均衡，QPS达到3200</p>

<h2>Redis相关接口优化</h2>

<p>找到一个仅包含一次Redis get操作且无返回数据的接口作为测试接口，在空接口优化之后的基础上进行测试，QPS：2000</p>

<p>对于连接另外的一个服务或中间件的情况，优化的方式并不多，最常用的优化方式就是提升连接数，在服务内部将连接Redis的连接数提MaxIdle提升到800，MaxActive提升到10000，QPS提升500，达到2500。</p>

<p>另外可以查看Redis服务的一些配置和性能图标，当前环境下使用的是AWS上的Redis服务，可配置的项目不多，使用的是两个节点的配置，在压测的过程中查看CPU占用、连接数占用、内存占用，均未发现达到上限，所以Redis服务没有可以优化的余地。</p>

<h2>数据库相关接口优化</h2>

<p>在高并发场景下，为了尽量提升QPS，查询的操作应该尽量全部使用Redis做缓存代替或者将请求尽量拦截在查询数据库之前，所以数据库的查询操作并不是QPS提升关注的重点。</p>

<p>但需要对数据库进行一些通用的优化，比如主从复制，读写分离、提升连接数、在大数据量下分表、优化SQL、建立索引。下面以一个查询操作为例，一条复杂的sql为例做一次SQL优化与索引建立，以单次的查询时间为目标进行优化。</p>

<h3>准备数据</h3>

<p>先用存储过程准备50w条数据
<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
</pre></td><td class='code'><pre><code class='sql'><span class='line'><span class="k">DELIMITER</span> <span class="err">$$</span>
</span><span class='line'><span class="k">CREATE</span> <span class="k">PROCEDURE</span> <span class="n">prepare_data</span><span class="p">()</span>
</span><span class='line'><span class="k">BEGIN</span>
</span><span class='line'>  <span class="k">DECLARE</span> <span class="n">i</span> <span class="nb">INT</span> <span class="k">DEFAULT</span> <span class="mi">1</span><span class="p">;</span><span class="o">&lt;/</span><span class="n">p</span><span class="o">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="o">&gt;</span>  <span class="n">WHILE</span> <span class="n">i</span> <span class="o">&amp;</span><span class="n">lt</span><span class="p">;</span> <span class="mi">500000</span> <span class="k">DO</span>
</span><span class='line'>    <span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">game_record</span> <span class="p">(</span><span class="n">app_id</span><span class="p">,</span> <span class="n">token</span><span class="p">,</span> <span class="n">game_match_id</span><span class="p">,</span> <span class="n">user_id</span><span class="p">,</span> <span class="n">nickname</span><span class="p">,</span> <span class="n">device_id</span><span class="p">,</span> <span class="n">prize_grade</span><span class="p">,</span> <span class="n">start_time</span><span class="p">,</span> <span class="n">score</span><span class="p">)</span> <span class="k">VALUES</span> <span class="p">(</span><span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="n">appid</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span> <span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="n">abc</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span> <span class="n">concat</span><span class="p">(</span><span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="n">xx</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span><span class="n">i</span><span class="p">),</span> <span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="mi">70961908</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span> <span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="n">asdfafd</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span> <span class="o">&amp;</span><span class="n">lsquo</span><span class="p">;</span><span class="n">xcao</span><span class="o">&amp;</span><span class="n">rsquo</span><span class="p">;,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">1543894842</span><span class="p">,</span> <span class="n">i</span> <span class="p">);</span>
</span><span class='line'>    <span class="k">SET</span> <span class="n">i</span> <span class="o">=</span> <span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
</span><span class='line'>  <span class="k">END</span> <span class="n">WHILE</span><span class="p">;</span>
</span><span class='line'><span class="k">END</span><span class="err">$$</span>
</span><span class='line'><span class="k">DELIMITER</span> <span class="p">;</span><span class="o">&lt;/</span><span class="n">p</span><span class="o">&gt;</span>
</span><span class='line'>
</span><span class='line'><span class="o">&lt;</span><span class="n">p</span><span class="o">&gt;</span><span class="k">call</span> <span class="n">prepare_data</span><span class="p">()</span>
</span></code></pre></td></tr></table></div></figure></p>

<h3>建立索引</h3>

<p>如何选择合适的列建立索引？</p>

<ol>
<li>WHERE / GROUP BY / ORDER BY / ON 的列</li>
<li>离散度大（不同的数据多）的列使用索引才有查询效率提升</li>
<li>索引字段越小越好，因为数据库按页存储的，如果每次查询IO读取的页越少查询效率越高</li>
</ol>


<p>对于以下SQL：
<code>sql
select * from game_record where app_id = ? and token=? and score != -1 order by prize_grade, score limit 50
</code>
优化前时间: 0.551s</p>

<p>对于order by操作，可以建立复合索引：
<code>sql
create index grade_and_score on game_record(prize_grade, score)
</code>
优化后时间: 0.194s</p>

<h3>优化SQL</h3>

<p>这里不贴具体的SQL语句了，以下是一些SQL通用优化方式：</p>

<ul>
<li>使用精确列名查询而不是*，特别是当数据量大的时候</li>
<li>减少子查询，使用Join替代</li>
<li>不用NOT IN，因为会使用全表扫描而不是索引；不用IS NULL，NOT IS NULL，因为会使索引、索引统计和值更加复杂，并且需要额外一个字节的存储空间。</li>
</ul>


<p>还有一些具体的数据库优化策略可以参考<a href="https://github.com/xbox1994/2018-Java-Interview/blob/master/MD/%E6%95%B0%E6%8D%AE%E5%BA%93-MySQL.md">这里</a></p>

<h2>TODO</h2>

<p>以上是仅仅是排除代码之后的服务和中间件压测，还应该加入代码逻辑进行更加全面的测试，然后对代码进行优化，具体优化方式请参考对应编程语言的优化方式。</p>

<p>如果以后碰到性能瓶颈，扩机器是最简单高效的，或者更换其他框架，或则还可以深入优化一下api gateway和后台服务交互的数据传输性能，因为这块是直接导致CPU达到瓶颈的原因。</p>

<h2>总结</h2>

<p>如果想做好一次压测和优化，需要非常清晰的思路、高效的压测方法、排查问题的套路、解决问题的方案，但最基础的还是需要知道一个请求从浏览器发送之后，到返回到浏览器，之间到底经历过什么，<a href="http://www.wangtianyi.top/blog/2017/10/22/cong-urlkai-shi-,ding-wei-shi-jie/">这篇比较基础的文章</a>可以帮你了解一些，但还不够，比如这次调优中还涉及到数据库优化、Go语言、硬件性能、AWS、Docker、K8S这样的云平台和容器技术、容器编排工具，所以压测调优是一次对自己掌握服务端整个架构和细节的考验和学习过程。</p>

<p>以上是本人和同事在短期内协作工作之后的总结，不足之处在所难免，欢迎各种意见。</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[从Java极速入门Go]]></title>
    <link href="http://www.wangtianyi.top/blog/2018/10/07/cong-javaji-su-ru-men-go/"/>
    <updated>2018-10-07T22:46:21+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2018/10/07/cong-javaji-su-ru-men-go</id>
    <content type="html"><![CDATA[<!-- more -->


<p>本文适合有Java开发经验且想学习Go的同学，是一篇非常适合入门Go语言的教程，本文只会涉及部分Go的核心内容，不适合想系统学习Go的同学，但也提供了系统学习的方式。</p>

<h1>Go语言简介</h1>

<p>下面是对Go的简单介绍，可以先了解一下，如果有哪些暂时无法理解的可以先跳过，学完【极速入门】之后回头来看就会恍然大悟。</p>

<p><img src="/images/blog/2018-10-07_1.jpg" title="&lsquo;image&rsquo;" ></p>

<h2>简介</h2>

<p>Go是一门语法非常简单的开源编程语言，目的是让程序开发变得更有生产效率。</p>

<p>Go是一门静态类型的编译型语言，通过在不同平台上的相应编译器来实现跨平台特性。</p>

<p>Go是一门广纳百家之长的语言，看起来像既像C++又像Delphi、Python，实则是吸收其他语言的优势，又针对其他语言的痛点进行设计。</p>

<p>它的并发机制使编写能够充分利用多线程的程序变得容易，其新颖的类型系统可实现灵活的模块化程序构建，是一门为大数据、微服务、高并发、云计算而生的通用语言。</p>

<h2>与Java对比</h2>

<ul>
<li>面向对象。Go不完全是面向对象的语言，因为没有【对象】的概念与关键字而是提供了结构体struct关键字，并且没有类型层次结构，比如没有继承、多态、泛型的支持。但是允许面向对象的编程风格，比如可以在struct里定义对象的内容，还能结合接口灵活实现。</li>
<li>接口。Go中的【接口】不是必须明确标注被实现才能使用某对象，而是使用<a href="https://studygolang.com/articles/214">duck typing</a>的方式提供了一种易于使用且更为通用的不同方法。结合上一点，所以Go语言在对于构建和使用对象时更加灵活</li>
<li>异常处理。无异常处理机制try catch，因为将异常耦合到业务代码中会导致代码更加复杂，并且倾向于定义出很多普通的错误。Go还可以利用函数多返回值来将可处理的异常作为一个返回值返回给调用者。</li>
<li>并发编程。Java中有很多针对多线程底层操作，如互斥，条件变量和内存屏障，这些过分强调低级细节，是的多线程编程难度较大。而Go中的【CSP并发模型】让开发人员不用关心这些细节，而是通过goruntime和在channel中传递消息的方式进行通信也能实现高效的并发程序</li>
<li>效率。程序执行速度远胜于Java，得益于Go是通过先编译链接后得到可执行文件的，而不是在运行时通过虚拟机解释运行。至于开发效率个人认为主要取决于对语言和相应框架使用的熟练程度。</li>
<li>部署。Go编译后生成的是一个可执行文件，除了glibc外没有任何外部依赖，完全不需要操行应用所需要的各种包、依赖关系，大大减轻了维护的负担</li>
<li>应用场景。Java的应用场景包括移动应用、应用服务器程序、客户端程序、嵌入式、大数据，可以说非常广，服务器到客户端、底层和应用层都能涵盖。Go则适合写服务器程序、大数据，以及基础设施程序如Docker、K8S，由于其语法简单、并发编程人性化、执行效率高所以在云计算领域中也是举足轻重的存在，但移动应用、嵌入式、客户端程序不是Go擅长的方向。</li>
</ul>


<h1>极速入门</h1>

<h2>环境搭建</h2>

<h3>Go语言安装</h3>

<p><a href="https://golang.org/dl/">https://golang.org/dl/</a> 选择1.10.3版本下载</p>

<h3>IDE安装</h3>

<p>IntelliJ IDEA中下载Go插件或者直接下载<a href="https://www.jetbrains.com/go/download/">GoLand</a></p>

<p>如果你不习惯Windows的快捷键设置，<a href="https://github.com/xbox1994/IDEA-Mac-Keymap-On-Windows">IDEA-Mac-Keymap-On-Windows</a>将Mac上的Command键改成左Alt键或许可以帮到你。</p>

<h2>HelloWorld</h2>

<p>让我们正式来体验Go语言，以下所有代码你应该先想一下大致应该如何写，然后看应该如何写，最后自己再写一遍直至跑通，有任何问题及时去搜索理解。</p>

<p>首先还是得从hello world开始。现在IDE中创建一个项目，在项目根目录下创建一个名为<code>helloworld.go</code>的文件，输入以下代码：</p>

<pre><code class="go">package main //必须是main包才能定义出一个可执行的入口，main命名与文件名无关

import "fmt" //一个格式化输出的核心库

func main(){ //函数命名格式：func 函数名(参数) 返回值
    fmt.Println("hello world!")
}
</code></pre>

<p>通过IDE的运行按钮或者在命令行中输入<code>go run helloworld.go</code>运行它</p>

<h2>SimpleSpider</h2>

<p>接下来介绍一个简单的爬虫来体验一部分Go语言的特性。不用马上动手实现它，而是从它开始简单看看Go的语法，Go的并发编程模型，等系统学习之后再来试试能不能实现。</p>

<p>这个爬虫的工作原理如下：首先准备一些想要抓取的网页Url作为种子传给Engine，由Engine负责流程控制，Engine会把Url传给Worker中的Fetcher来下载具体的HTML文件，然后发送给Url对应的Parser解析函数得到想要抓取的信息以及当前页面上可以继续当做种子再次抓取的Url，最后将Url与对应的Parser传给Engine继续抓取。</p>

<p>其中支持并发的Worker与单个Engine传递与接收数据使用的是go runtime与channel实现，可以来感受一下前所未有的并发编程体验。Worker数量越多，爬虫效率越高。</p>

<p><img src="/images/blog/2018-10-07_2.jpg" title="&lsquo;image&rsquo;" ></p>

<p>先在根目录下将不同模块的文件夹创建出来，有engine, fetcher, parser三个，然后在根目录把入口程序spider.go写好：</p>

<pre><code class="go">func main() {
    engine.SimpleEngine{  // engine是我们需要实现的一个程序包，创建engine包中的SimpleEngine结构
        WorkerCount: 100, // worker开启数量
    }.Run(engine.Request{ // 被调用的Run方法需要被定义在该结构体的内部
        Url:        "http://www.zhenai.com/zhenghun", //第一个参数是用于被抓取的网页的URL
        ParserFunc: parser.ParseCityList, //第二个参数是对于不同Url编写的不同的解析器
    })
}
</code></pre>

<p>在engine文件夹中新建types.go将Request结构体定义：</p>

<pre><code class="go">type Request struct {
    Url        string
    ParserFunc func([]byte) ParseResult // 解析函数，不同的Url可能对应不同的解析函数，传入HTML的字节数组
                                        // 传出HTML中解析出来的可以继续当成种子的Request和HTML中的有效信息
}

type ParseResult struct {
    Requests []Request //从HTML中解析出来的种子
    Items    []interface{} //任意类型的数组结构
}
</code></pre>

<p>接下来了解一下goruntime与channel。goroutine是Go里的一种轻量级线程——协程。相对线程，协程的优势就在于它非常轻量级，进行上下文切换的代价非常的小，通过go关键字传入一个方法就能开启一个协程。</p>

<p>channel是一种带有类型的变量类型，你可以通过它用channel操作符 <code>&lt;-</code> 在不同的协程之间来发送或者接收值。默认情况下，发送和接收操作在另一端准备好之前都会阻塞。这使得 Go 程可以在没有显式的锁或竞态变量的情况下进行同步，从而大大降低了并发编程的难度，提升了开发效率。</p>

<p>接下来看engine中的核心代码：</p>

<pre><code class="go">type SimpleEngine struct { // 创建engine结构体用以在创建时接受参数，但这并不是构造函数，Go中没有构造函数与析构函数
    WorkerCount int
}

// 为SimpleEngine结构体定义Run方法。SimpleEngine为该方法的值接受者。
// 由于方法经常需要修改它的接收者，指针接收者比值接收者更常用；参数是可变长的类型，用来传入多个种子
func (e SimpleEngine) Run(seeds ...Request) {
    in := make(chan Request) // 创建种子输入channel，其中可以传递的数据为Request，worker将从这里取，engine将种子往这里放
    out := make(chan ParseResult) // 种子输出channel，其中可以传递的数据为ParseResult，worker将解析结果传到这里交给engine

    // 开启若干个worker协程，等待种子被放入
    for i := 0; i &lt; e.WorkerCount; i++ {
        go func() {
            for {
                // 开始时所有worker都被阻塞，等待种子被传入
                r := &lt;-in // 创建r变量，初始值从输入channel中获取
                // 收到某个种子，开始解析
                result, e := worker(r)
                if e != nil {
                    continue
                }
                // 将任务执行结果传回engine，但是这里如果直接写out &lt;- result会无法执行，因为有循环等待产生，原因：
                // 想要执行这行代码必须需要有engine在执行①开始等待才行，但是engine执行这行代码的前提完成②的输入
                // 也就是需要有空闲的worker在等待接受in，然而此时worker正在执行本行命令
                go func() {out &lt;- result}()
            }
        }()
    }

    // 传入种子到输入channel
    for _, request := range seeds {
        in &lt;- request
    }

    for { // 循环结构只有for关键字，这里是当成while(true)使用，让engine一直运行，等待worker返回的数据
①     parseResult := &lt;- out

        // 现在是打印得到的数据，以后将改为保存到数据库
        for _, item := range parseResult.Items { // 相当于foreach，第一个返回值是index，第二个是object，_代表不使用
            log.Printf("Got item %v\n", item)
        }

        // 存储后将后续种子发送给输入channel
        for _, r := range parseResult.Requests {
②         in &lt;- r
        }
    }
}

func worker(r Request) (ParseResult, error) {
    log.Printf("Fetching %s", r.Url)
    // 下载html内容
    body, e := fetcher.Fetch(r.Url)
    if e != nil {
        log.Printf("Fetcher error with url: %s. %v", r.Url, e)
        return ParseResult{}, e
    }

    // 根据request里提供的解析方法解析当前html内容成为result放入队列
    return r.ParserFunc(body), nil
}
</code></pre>

<p>核心代码就是以上，但还需要实现负责下载的fetcher和负责解析的parser，完整代码可查看<a href="https://github.com/xbox1994/GoCrawler">https://github.com/xbox1994/GoCrawler</a>中的crawler_standalone学习</p>

<h3>依赖包安装</h3>

<p>由于GFW的关系，当你在安装某个依赖包时，可能被挡住，推荐按以下的顺序来尝试安装go的依赖包（xxx代表包的地址）
1. <code>go get xxx</code>
2. 使用gopm安装，先下载gopm：<code>go get -v -u github.com/gpmgo/gopm</code>，然后<code>gopm get -g -v xxx</code>
3. 去对应的github主页上，<code>git clone</code>下来并放到GOROOT或GOPATH目录的src文件夹下，并将下载的文件夹命名为包的地址</p>

<h2>Beego</h2>

<p>Beego是国人开发的一款Go web框架，这里查看相关<a href="https://beego.me/docs/intro/">简介</a></p>

<p>它的功能全面、结构清晰、上手容易、有一定社区活跃度但代码质量不高，适合快速开发。</p>

<p>接下来请按照官网<a href="https://beego.me/quickstart">quickstart</a>的例子来快速体验一把。</p>

<p>在这个例子中要注意的是，Go的方法参数传递只有值传递，没有引用传递，也就是如果传递一个任意类型的变量给某个方法，那么这个变量将被复制一份传给函数内部，所以在函数内部对这个变量进行操作则是对原变量的副本进行操作，如果想直接操作原变量的内容，使用&amp;将地址传进去即可。</p>

<h2>系统学习推荐</h2>

<p>本文仅仅是用到了小部分Go的语法和功能特性来尝试带领你入门，还有很多值得了解的比如数组的切片、duck typing 接口、反射，希望你看完之后一定不要止步于此，一切都才刚刚开始，下一步就是开始进行系统的学习了。</p>

<p>这个<a href="https://www.zhihu.com/question/30461290">知乎回答</a>可能对你有所帮助，但很不幸给的链接很多都需要翻墙。</p>

<h2>参考</h2>

<p><a href="https://coding.imooc.com/learn/list/180.html">https://coding.imooc.com/learn/list/180.html</a><br/>
<a href="https://golang.org/doc/faq">https://golang.org/doc/faq</a><br/>
<a href="https://www.zhihu.com/question/21409296">https://www.zhihu.com/question/21409296</a><br/>
<a href="https://beego.me/quickstart">https://beego.me/quickstart</a><br/>
<a href="https://tour.go-zh.org">https://tour.go-zh.org</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[系统设计面试题：综合考察面试者的大招]]></title>
    <link href="http://www.wangtianyi.top/blog/2018/08/31/xi-tong-she-ji-mian-shi-ti-zong-he-kao-cha-mian-shi-zhe-de-da-zhao/"/>
    <updated>2018-08-31T16:21:22+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2018/08/31/xi-tong-she-ji-mian-shi-ti-zong-he-kao-cha-mian-shi-zhe-de-da-zhao</id>
    <content type="html"><![CDATA[<p>站在面试官的角度，如何设计一道系统面试题，如何考察面试者的一整套流程，以及相关解答</p>

<!-- more -->


<p>在高级程序员面试的过程中，系统设计是一个很可能被问到的一个问题，由于它能体现出面试者很多方面的能力，是一道非常考验综合素质的面试题，一直被面试官青睐。</p>

<p>比如：业务理解抽象能力、业务挖掘能力、沟通表达能力、高层系统设计能力、底层服务实现经验、全栈经验</p>

<p>下面用一个自己设计的非常简单的系统设计题来一起体会体会。本人经验也不算多，仅供参考。</p>

<h2>提出业务需求</h2>

<p>首先面试官必须向面试者将业务讲清楚，比如要做的是什么，给哪些人用（有哪些角色），主要流程是怎样的，有什么系统上的要求，这一阶段同时也需要面试官有一定业务描述能力。最好是当场结合白板画出来讲给面试者听。</p>

<p>在本文中将以一个非常简单的<strong>客服系统</strong>为例，比如在面试时我会把下面这些告诉面试者：</p>

<ul>
<li>角色：用户、客服人员</li>
<li>流程：创建工单 - 客服领取工单 - 回复 - 解决/回复</li>
<li>要求：工单的更新实时推送到用户与客服、支持工单内容实时的全文检索功能</li>
</ul>


<p>把系统的各个模块都画出来，最好从客户端开始，将所有涉及到的中间件、自己搭建的核心服务、第三方平台，并展示它们的关系。给你10分钟完成。</p>

<h2>与面试者沟通</h2>

<p>面试者在拿到这个题目之后肯定会问一些问题，比如需要支持高并发吗？需要将所有的服务都画出来吗？你可以回答，能支持一定量的高并发更好，不需要把所有服务都画出来，仅仅画出实现整个架构的核心服务就行。</p>

<p>然后面试者开始思考，开始设计，然后画出来了下面的一张图给你，并把他的设计讲给你听。</p>

<p><img src="/images/blog/2018-08-31_1.png" title="&lsquo;image&rsquo;" ></p>

<ol>
<li>从客户端开始，可以使用客户端的缓存拿到上次已经加载过的资源，或者通过CDN拿到一些静态资源如JS、CSS、常用流媒体文件，使用阿里云或AWS的CDN就行，Object Store用的也是对应的对象存储服务。然后通过负载均衡器组访问后端服务器组Web Server，Web Server往下的所有部分都属于Web Service。</li>
<li>Read API，对应着某个工单全部信息查询的API，需要通过访问Ticket Service判断是否保存在缓存中，如果没有保存在缓存里就访问数据库只读副本拿到数据，这里对数据库做的读写分离是为了在高并发场景下的性能优化。</li>
<li>Write API，对应创建、修改、删除工单的API，由于写操作伴随着工单更新状态的推送，以及需要更新全文检索查询的索引来保证一定的全文检索的实时性，所以这两个操作使用队列完成，每个API只需要同步写数据库而已。至于推送的实现，建议使用极光、个推的第三方平台实现，因为不同的终端实现不同且复杂，服务端的长连接要求也比较高。</li>
<li>Search API，对应全文检索API，会在Search Service中请求Elastic Search服务来查询需要的数据。在Search Service也需要安排一个定时任务，定期从数据库只读副本中读取增量数据来重建索引，保证索引建立无差错，同时也能保证日后的数据导入或迁入能够建立索引</li>
<li>图中只是画出了针对业务的核心服务，省略了很多微服务体系中的关键角色如服务发现注册中心、配置中心、包含鉴权的网关。在部署的时候还需要结合容器化技术和容器管理平台做到多个节点同时服务以保障高可用性和扩展性。</li>
</ol>


<h2>询问底层实现</h2>

<p>如果能在当场完成这样一张图的话个人觉得整个面试者在高层设计上已经是比较优秀的了，他对业务的理解，各种中间件的使用，全栈经验都比较不错。但是往往面试者不能设计出这样完整的架构，有个80%就已经不错了。所以我们面试官在设计题目的时候一定要自己写一份比较完整的答案给自己做参考，然后给面试者打分。</p>

<p>接下来就根据时间和你想考察的重点来问几个底层实现的问题了，举几个例子。</p>

<h3>工单附件存储方式</h3>

<p>问：如果要你考虑到工单的附件可以是图片、视频的话，应该如何设计？</p>

<p>答：首先图片、视频的传输是比较消耗网络资源的，不能交给Ticket Service，让Ticket Service专注负责业务逻辑的处理，所以需要自己实现一个文件服务器或者使用云服务中的对象存储。</p>

<h3>扩展性</h3>

<p>问：如何评价你设计的系统的扩展性？</p>

<p>答：扩展性是系统在面临某个瓶颈之后需要对系统进行添加成员来突破瓶颈时的扩展能力。如果扩展性好，那么添加中间件（垂直扩展）、添加节点（水平扩展）都会比较方便。图中如果不看CDN、缓存这两部分，那么一旦遇到性能问题，添加这两部分来解决问题也是非常容易的，因为我们的客户端是可配置的，我们的服务端也能支持缓存，当然这并不能体现出设计的好处，如果看水平扩展就好看了，如果要对数据库进行水平扩展，原有数据库已经是主从复制的架构，那么想添加主、从节点也是非常容易的。如果要对服务节点进行水平扩展，那么就需要在部署的方面去考虑了，比如用容器化技术将服务打包成镜像，用容器管理平台管理容器运行状态与节点自动伸缩。</p>

<h3>高可用性</h3>

<p>问：如何评价你设计的系统的高可用性？</p>

<p>答：这是设计图中无法表现的，但是可以通过其他的方式来说说我的理解。对于服务节点发生的故障，首先服务注册中心就无法收到心跳回应了，容器管理平台也会监测到服务运行状态异常，那么请求就不会再发向这个服务节点，而是转向正常的服务节点，主要是需要提前配置好监控方案已经故障转移方案。</p>

<h3>一致性</h3>

<p>问：你设计的系统中有什么数据不一致的地方？如何解决？</p>

<p>答：数据库与缓存不一致（根据业务需求决定，本系统并不需要达到实时的数据交互能力，所以使用有过期时间的缓存就行）、数据库与Elastic Search不一致（每次对工单信息的修改就需要将工单信息更新到Elastic Search中，但也会可能有失败，所以有一个定时任务定时增量更新一段时间内的数据到ES）</p>

<h3>如何保证高并发能力</h3>

<p>答：前端使用缓存与CDN、每个服务都有多个服务节点支撑以及对应的负载均衡、将热点数据通过放入缓存中提高抓取数据的性能、将复杂操作使用队列异步执行任务、数据库读写分离对读与写操作分别进行数据库层面上的优化</p>

<p>由于本人在底层实现方面经验不够，底层实现部分的方案并不一定是最好的方案，我也会在与他人交流之后继续优化这些方案。另外，欢迎客官在评论区提出你的想法，我们一起来优化~</p>

<h2>参考</h2>

<p><a href="https://github.com/donnemartin/system-design-primer/blob/master/README-zh-Hans.md">https://github.com/donnemartin/system-design-primer/blob/master/README-zh-Hans.md</a></p>

<h2>号外号外</h2>

<p>最近在总结一些针对<strong>Java</strong>面试相关的知识点，感兴趣的朋友可以一起维护~<br/>
地址：<a href="https://github.com/xbox1994/2018-Java-Interview">https://github.com/xbox1994/2018-Java-Interview</a></p>
]]></content>
  </entry>
  
</feed>
