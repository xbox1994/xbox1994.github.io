<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: 架构 | 干掉这里的Bug]]></title>
  <link href="http://www.wangtianyi.top/blog/categories/jia-gou/atom.xml" rel="self"/>
  <link href="http://www.wangtianyi.top/"/>
  <updated>2021-06-26T14:40:14+08:00</updated>
  <id>http://www.wangtianyi.top/</id>
  <author>
    <name><![CDATA[Tianyi Wang]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[【架构入门】思维导图]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/06/08/jia-gou-ru-men-end/"/>
    <updated>2019-06-08T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/06/08/jia-gou-ru-men-end</id>
    <content type="html"><![CDATA[<!-- more -->


<p>看完大部分《从零开始学架构》深有感触，作者把架构的知识点融会贯通，但究竟能让自己学到才是硬道理，思维导图原本觉得没什么用的，不过在总结知识点，以后回顾的方面挺棒，这才是把书读薄</p>

<p><img src="/images/blog/2019-06-08_0.png" title="&lsquo;image&rsquo;" ></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【架构入门 - 可扩展篇】]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/05/18/jia-gou-ru-men-ke-kuo-zhan-pian/"/>
    <updated>2019-05-18T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/05/18/jia-gou-ru-men-ke-kuo-zhan-pian</id>
    <content type="html"><![CDATA[<p>随着服务的壮大，使用人数的增多，业务的递增，服务的扩展性尤为关键，在不影响现有架构的情况下如何增加机器、扩展功能？</p>

<!-- more -->


<p><img src="/images/blog/2019-05-18_0.png" title="&lsquo;image&rsquo;" ></p>

<h2>基本思想和模式</h2>

<p>一个字：<strong>拆</strong>。把大的系统拆为小的系统，下面是拆分的几个不同方法，也是拆分依赖的不同维度，以学生信息管理系统为例：</p>

<ul>
<li>面向流程拆分。将整个业务流程拆分为几个阶段，比如展示层、业务层、数据层、存储层</li>
<li>面向服务拆分。将系统提供的服务拆分，比如注册登录服务、信息管理服务</li>
<li>面向功能拆分。将系统提供的功能拆分，比如将注册登录服务拆得更细致，从而有手机号注册登录、邮箱注册登录</li>
</ul>


<p>那么我们作为架构师，应该如何选择呢？就是需要明白不同拆分方式的优缺点，适用的场景：</p>

<ul>
<li>面向流程拆分。扩展时大部分情况只需要修改某一层的时候。比如将存储层从MySQL扩展为同时支持Oracle，就只需要扩展存储层和数据层即可</li>
<li>面向服务拆分。扩展时大部分情况只需要对某个服务扩展，或增加新的服务时。比如要增加身份证注册功能，只需要修改注册服务</li>
<li>面向功能拆分。扩展时大部分情况只需要对某个功能扩展，或增加新的功能时。比如要增加身份证注册功能，无需修改任何服务，只需要增加一个新的功能模块即可</li>
</ul>


<h2>面向流程拆分：分层</h2>

<p>很常见的架构模式，也叫N层架构，N至少是2，比如：</p>

<p>1）C/S、B/S架构</p>

<p>划分的对象时整个业务系统，将和用户交互的部分独立为一层，后台作为另外一层，如图：</p>

<p><img src="/images/blog/2019-05-18_1.png" title="&lsquo;image&rsquo;" ></p>

<p>2）MVC架构</p>

<p>划分的对象是单个业务系统，将不同的职责划分开</p>

<p><img src="/images/blog/2019-05-18_2.png" title="&lsquo;image&rsquo;" ></p>

<p>3）逻辑分层架构</p>

<p>划分的对象可以是单个业务子系统，也可以是整个业务系统，将不同的职责划分开，与MVC架构不同在于逻辑分层架构中的层是自顶向下依赖的。比如操作系统内核架构、TCP/IP架构，比如安卓操作系统：</p>

<p><img src="/images/blog/2019-05-18_3.png" title="&lsquo;image&rsquo;" ></p>

<p>分层架构的核心在于：<strong>保证各层之间的职责差异足够清晰</strong>。之所以能够支撑系统扩展，本质在于隔离关注点，就是每层中的组件只处理本层的逻辑。这样就可以支持系统在某层上的快速扩展</p>

<p>但是除开分层，还需要保证层与层之间的依赖是稳定的，才能真正支撑快速扩展，这个依赖就是接口</p>

<p><img src="/images/blog/2019-05-18_4.png" title="&lsquo;image&rsquo;" ></p>

<p>Linux内核为支撑不同的文件系统格式，抽象出来VFS文件系统接口，如上。如果没有VFS，那么即使增加一个文件系统，也无法将依赖文件系统的系统应用上新的文件系统，因为并不知道接口是什么，而VFS统一了</p>

<p>优势：各层之间自顶而下结构清晰、强制约束层之间的关系是两两依赖，依赖的数量固定<br/>
缺点：如果层级多，强制两两依赖将导致性能、实现上的浪费，架构也会混乱<br/>
场景：适用互联网架构整体分层、操作系统分层，不适合互联网架构后台服务</p>

<h2>面向服务拆分：SOA、微服务</h2>

<h4>SOA</h4>

<p>Service Oriented Architecture，面向服务的架构，是上世纪90年代就已经出现的架构模式：</p>

<ol>
<li>服务。所有业务都是一项服务，服务就要对外提供开发的能力，其他系统可以直接使用</li>
<li>ESB，Enterprise Service Bus，企业服务总线，类似于计算机总线，ESB将企业中各个不同的服务连接在一起，并对外提供各式各样需要的接口。功能强大，但需要完成非常多的协议和数据格式的相互转换，工作量巨大，且耗费大量计算性能，容易成为系统的性能瓶颈</li>
</ol>


<p><img src="/images/blog/2019-05-18_5.png" title="&lsquo;image&rsquo;" ></p>

<h3>微服务</h3>

<p>微服务这几年很火，但也在早就出来了，只不过Martin Fowler将它发扬光大</p>

<p>与SOA的区别：</p>

<ol>
<li>服务粒度。微服务更细，SOA对应的是"员工管理系统"，微服务是"员工管理系统-员工信息管理"服务</li>
<li>服务通信。微服务仅仅使用统一的消息格式如RESTFul、RPC就好了，而SOA的ESB将服务定义、路由、消息转换、传递都给实现了，是重量级的</li>
<li>服务交付。微服务的交付理念是"持续交付"，需要使用自动化测试、持续集成、自动化部署的敏捷最佳时间，而SOA没有要求</li>
<li>应用场景。微服务适合快速、轻量级的Web互联网系统，而SOA适合庞大、复杂、异构的企业级系统</li>
</ol>


<p>但微服务的坑是相当的多，稍有不慎就会陷入焦油坑，最后发现某些情况下几个单体应用的架构甚至甩了微服务好几条街</p>

<p>比如：服务划分过细导致服务关系复杂、团队开发效率下降、调用链太长性能下降、定位问题困难、对运维自动化要求极高、微服务数量递增管理难度极大&hellip;从而步了SOA的后尘</p>

<p>更多微服务资料可参看：<a href="https://www.wangtianyi.top/blog/2017/04/16/microservies-1-introduction-to-microservies/">https://www.wangtianyi.top/blog/2017/04/16/microservies-1-introduction-to-microservies/</a></p>

<h2>面向功能拆分：微内核</h2>

<p>包含两类组件：</p>

<ol>
<li>核心系统。负责系统的功能组件如模块加载，模块间通信</li>
<li>插件模块。实现具体的业务逻辑比如手机号注册功能</li>
</ol>


<p><img src="/images/blog/2019-05-18_6.png" title="&lsquo;image&rsquo;" ></p>

<p>比如在规则引擎架构上有以下体现，比如电商促销中会有很多规则，不可能把规则都写到代码里，那样完全无法有效扩展，架构如下：</p>

<p><img src="/images/blog/2019-05-18_7.png" title="&lsquo;image&rsquo;" ></p>

<p>设计关键点在于：</p>

<ol>
<li>插件管理。数据库</li>
<li>插件连接。自己开发的规则引擎维护连接方式</li>
<li>插件通信。数据流或事件流，规则之间不需要相互依赖，只要向规则引擎输出内容</li>
</ol>


<h2>参考</h2>

<p><a href="https://docs.nginx.com/nginx/admin-guide/load-balancer/http-health-check/">https://docs.nginx.com/nginx/admin-guide/load-balancer/http-health-check/</a></p>

<h2>号外号外</h2>

<p>最近在总结一些针对<strong>Java</strong>面试相关的知识点，感兴趣的朋友可以一起维护~<br/>
地址：<a href="https://github.com/xbox1994/2018-Java-Interview">https://github.com/xbox1994/2018-Java-Interview</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【架构入门 - 高可用篇】计算高可用]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/05/15/jia-gou-ru-men-gao-ke-yong-pian-ji-suan-gao-ke-yong/"/>
    <updated>2019-05-15T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/05/15/jia-gou-ru-men-gao-ke-yong-pian-ji-suan-gao-ke-yong</id>
    <content type="html"><![CDATA[<p>计算高可用的目标是当部分服务器停止服务时，整体的计算任务能够继续进行，所以本质是通过冗余来规避风险</p>

<!-- more -->


<p><img src="/images/blog/2019-05-15_1.jpeg" title="&lsquo;image&rsquo;" ></p>

<h2>问题</h2>

<p>计算高可用的复杂度体现在<strong>任务管理</strong>方面，比如一台机器挂了，那么上面的任务如何处理？所以设计高可用架构有以下问题：</p>

<ol>
<li>哪些服务器可以执行任务？第一种方式是每个服务器都能执行任务；第二种方式是特定主机可以执行任务，如etcd的leader</li>
<li>任务如何重新执行？第一种是即使任务失败也不处理，但要保证新的任务分配到其他正常机器上执行；第二种是通过任务管理器来管理计算任务，当任务执行完成之后反馈给管理器来决定是否将任务重新分配到其他服务器上执行</li>
</ol>


<p>那么针对以上问题，有以下几种常见架构可供选择</p>

<h2>主备</h2>

<p>主备的架构：</p>

<ul>
<li>主机执行所有计算任务。备机上的业务系统已经准备好启动环境（冷备）或正在运行业务系统（热备）</li>
<li>主机故障时，将计算任务继续发送给主机，此时系统部分不可用</li>
<li>如果主机恢复了，任务分配器继续发送任务给主机，如果不能恢复，则需要人工操作，将备机升级为主机，还需要拿掉原来的主机，且增加一台备机</li>
</ul>


<p>优点：简单，主备、任务分配器之间不需要交互来判断状态和自动切换的配置<br/>
缺点：人工操作不可控、系统挂掉的可能性大、备机性能的浪费<br/>
适合：使用人数不多，不太重要的后台管理系统</p>

<h2>主从</h2>

<p>主从的架构：</p>

<ul>
<li>主机与从机同时执行计算任务</li>
<li>主机故障时，将计算任务继续发送给主机，此时系统部分不可用</li>
<li>如果主机不能恢复，需要人工操作，将从机升级为主机，拿掉原主机，增加新从机</li>
</ul>


<p>优点：系统稳定性得到部分提升、所有机器的性能充分发挥<br/>
缺点：人工操作不可控、系统挂掉的可能性大、需要对任务分配器进行任务分配的配置<br/>
适合：使用人数较多，比较重要的后台管理系统</p>

<h2>对称集群</h2>

<p>首先定义一下集群的分类：<br/>
对称集群，就是集群中每个服务器都是执行的相同的任务<br/>
非对称集群，就是集群中的每个服务器的角色可能不同，执行不同的任务</p>

<p>主备与主从的最大的问题在于只有固定的主备和主从两个（组）服务器，需要人工干预进行增减，不能保证服务的可用性。所以在集群方案里，系统需要完成自动切换操作</p>

<p>对称集群的架构：</p>

<ul>
<li>所有机器同时执行计算任务</li>
<li>某台机器故障时，将计算任务停止发送给它，而是发给其他机器</li>
</ul>


<p>这里的复杂度在于：</p>

<p>1） 任务分配器如何分配任务？<br/>
在之前的<a href="https://www.wangtianyi.top/blog/2019/05/01/jia-gou-ru-men-gao-xing-neng-pian-ji-qun/">【架构入门 - 高性能篇】集群高性能</a>已经讲到过负载均衡算法，这里不废话了</p>

<p>2） 任务分配器如何检测服务器状态？<br/>
一般是根据任务分配器与服务器之间传递心跳包、或者任务分配器对于发送给服务器的请求做记录，比如一段时间内超过多少个错误请求来检测的，如Nginx的配置：</p>

<pre><code class="text">upstream backend {
    server backend1.example.com;
    server backend2.example.com max_fails=3 fail_timeout=30s;
}
</code></pre>

<p>如果NGINX未能在30秒内向服务器发送请求或未收到响应3次，则表明服务器在30秒内不可用。如果本次请求中，节点正好出现故障，Nginx将把请求转交给接下来的节点处理。所以不会影响到这次请求的正常进行。</p>

<p>优点：系统稳定性高、所有机器的性能充分发挥<br/>
缺点：复杂度高<br/>
适合：在线服务</p>

<h2>非对称集群</h2>

<p>非对称集群的架构：</p>

<ul>
<li>集群中用某种方式来区分不同服务器的角色。如取当前存活服务器中节点ID最小的服务器作为master服务器</li>
<li>任务分配器将不同的任务发送到不同服务器。如将A任务发送给master服务器，将B任务发送给slave服务器</li>
<li>当服务器故障时，需要重新分配角色</li>
</ul>


<p>对比对称集群，复杂度更高：</p>

<ol>
<li>任务分配策略更复杂，需要将任务划分成不同类型到不同角色节点</li>
<li>角色分配策略也复杂，需要ZAB/Raft算法实现Leader选举</li>
</ol>


<p>比如在Zookeeper中：</p>

<ul>
<li>任务分配器：每个节点都是任务分配器，Follower角色收到请求后判断，如果是写请求就发送给Leader角色，如果是读请求就自己处理</li>
<li>角色分配：ZAB算法选举Leader，当Leader故障后，所有Follower角色会暂停写操作，然后开始选举，直到新的Leader选举出来之后才继续提供服务</li>
</ul>


<p>优点：系统稳定性高、所有机器的性能充分发挥<br/>
缺点：复杂度高<br/>
适合：大规模集群内部高性能分布式协调管理中心</p>

<h2>参考</h2>

<p><a href="https://time.geekbang.org/column/intro/81">https://time.geekbang.org/column/intro/81</a><br/>
<a href="https://docs.nginx.com/nginx/admin-guide/load-balancer/http-health-check/">https://docs.nginx.com/nginx/admin-guide/load-balancer/http-health-check/</a></p>

<h2>号外号外</h2>

<p>最近在总结一些针对<strong>Java</strong>面试相关的知识点，感兴趣的朋友可以一起维护~<br/>
地址：<a href="https://github.com/xbox1994/2018-Java-Interview">https://github.com/xbox1994/2018-Java-Interview</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【架构入门 - 高性能篇】数据库高性能]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/05/05/jia-gou-ru-men-gao-xing-neng-pian-shu-ju-ku/"/>
    <updated>2019-05-05T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/05/05/jia-gou-ru-men-gao-xing-neng-pian-shu-ju-ku</id>
    <content type="html"><![CDATA[<p>单机高性能与集群高性能都是针对负责计算的服务器而言的，负责存储的数据库服务器因为处理的是数据而不是计算，架构和使用方式又有所不同</p>

<!-- more -->


<p><img src="/images/blog/2019-05-05_1.jpg" title="&lsquo;image&rsquo;" ></p>

<h2>SQL - 读写分离</h2>

<p>基本原理是将数据库读写操作分散到不同节点上，从而分散读写压力到不同的节点上</p>

<p><img src="/images/blog/2019-05-05_2.jpg" title="&lsquo;image&rsquo;" ></p>

<p>一主一从或一主多从都可以；主机负责读写操作，从机负责读操作，每个机器都需要同时工作；主机通过复制将数据同步到从机，每个节点都存储了所有的数据</p>

<h4>复制延迟</h4>

<p>MySQL的主从复制延迟可能达到1秒，如果有大量数据，1分钟也可能，那么这样很容易造成用户注册了但是无法登陆</p>

<p>一般的解决方法是将关键业务全部指向主机，非关键业务进行读写分离，比如注册登陆都指向主机，修改个人信息就读写分离，即使查询出来的是旧的数据，业务上的影响也不会太大</p>

<h4>分配机制</h4>

<p>如何将读写操作分开，访问不同的数据库呢？</p>

<p>【1】 程序代码封装</p>

<p>在程序中抽象出一个数据访问层，实现读写操作分离和数据库服务连接的管理，比如</p>

<p><img src="/images/blog/2019-05-05_3.jpg" title="&lsquo;image&rsquo;" ></p>

<p>实现简单，但无法通用，每个语言都需要写一个，主从数据库发生切换则需要修改所有系统并重启</p>

<p>【2】 中间件封装</p>

<p>独立出一套系统来，实现读写操作分离和数据库服务连接的管理，中间件对业务服务器提供SQL兼容的协议。业务服务器访问中间件与数据库无差异，如图</p>

<p><img src="/images/blog/2019-05-05_4.jpg" title="&lsquo;image&rsquo;" ></p>

<p>能够支持多种语言，且能够探测服务器主从状态，比如向某个测试表里写个数据，成功就是主机，失败就是从机。但是对应的实现极其复杂，容易出BUG，所有数据库请求都走中间件的话性能是个大问题。一般使用成熟的中间件方案，如MySQL Proxy，Atlas</p>

<h2>SQL - 分库分表分区</h2>

<p>为什么需要分这么多东西，主要是因为如果在单库或者单表上：</p>

<ul>
<li>数据量太大，即使加了索引，索引也非常大，读写性能太差</li>
<li>数据量太大，备份和恢复耗时太长</li>
<li>数据库文件太大，导致物理上受影响的可能更大（机房火灾烧了一块，恰好是这个数据库）</li>
</ul>


<h4>分库</h4>

<p>为了解决数据量太大，且读写操作太多的问题，分库是按不同的业务模块将数据分散到不同的数据库服务器，比如：</p>

<p><img src="/images/blog/2019-05-05_5.jpg" title="&lsquo;image&rsquo;" ></p>

<p>但会存在以下问题：</p>

<ol>
<li>事务问题。在执行分库之后，由于数据存储到了不同的库上，数据库事务管理出现了困难。如果依赖数据库本身的分布式事务管理功能去执行事务，将付出高昂的性能代价；如果由应用程序去协助控制，形成程序逻辑上的事务，又会造成编程方面的负担</li>
<li>跨库跨表的join问题。在执行了分库分表之后，难以避免会将原本逻辑关联性很强的数据划分到不同的表、不同的库上，我们无法join位于不同分库的表，也无法join分表粒度不同的表，结果原本一次查询能够完成的业务，可能需要多次查询才能完成</li>
<li>额外的数据管理负担和数据运算压力。额外的数据管理负担，最显而易见的就是数据的定位问题和数据的增删改查的重复执行问题，这些都可以通过应用程序解决，但必然引起额外的逻辑运算</li>
<li>成本问题。本来是1台服务器，现在是3台，所以一开始是不需要考虑分库的，除非一开始就是业务特别多，用户量特别大，否则一定要避免初期在开发上的无端开销，以后业务起来之后再分库也不迟</li>
</ol>


<h4>分表</h4>

<p>【1】 垂直分表</p>

<p>为了解决表的宽度问题，同时还能分别优化每张单表的处理能力。所以将表结构根据数据的活跃度拆分成多个表，把不常用的字段单独放到一个表、把大字段单独放到一个表、把经常使用的字段放到一个表</p>

<p>复杂度的增加在于操作表的数量会增加，比如以前一条SQL需要查一个表，现在需要查两次，分别查两个表</p>

<p>【2】 水平分表</p>

<p>为了解决单表数据量过大（数据量达到千万级别）问题。所以需要将数据按一定的规则划分到不同表中，需要在读写的时候通过规则路由到具体的数据上</p>

<p>从而引起的复杂度有：</p>

<ul>
<li>路由</li>
</ul>


<p>范围路由：根据有序的数据列，如时间戳/ID作为路由条件，不同的分段分散到不同的表中。比如按ID分段<br/>
复杂度在于分段大小，太小则导致表太多影响以后维护难度，太大导致单表性能问题，一般在100万到2000万之间<br/>
范围路由的优点在于可以跟随数据的增长而平滑地去扩充新的表，而不影响原有表的数据，但缺点在于数据分布不均匀，可能0-100万的用户表已经填满了，但100-200万的用户还没几个，并且还需要维护表的增加逻辑</p>

<p>Hash路由：通过选取某个列的值进行Hash运算，然后根据Hash结果分散到不同的表中。比如ID字段用ID%n（n代表分表的数量）计算，从而路由到对应表中<br/>
复杂度一是在于表的数量选取，二是在于路由字段的选取，因为还需要考虑到日后的增删改查，如果数据分散到100张表里，那么不得不查100张表才能拿到所有数据，所以还需要在一定程度上将数据聚合，比如根据用户组的ID进行分表，那么就不会影响到用户组的查询效率，当然到底通过哪个字段分，是根据业务需要来取舍的<br/>
Hash路由的优点在于实现简单，不需要维护扩表逻辑，一般也不需要扩表，缺点在于一旦要扩展表的时候，所以数据都需要重新计算分配，导致大量数据的变动</p>

<p>配置路由：用一张独立的路由表来记录路由信息。比如以用户ID为路由字段，创建一张维护用户ID-表ID信息的表，做到根据用户ID，查到这个用户信息所在的表<br/>
配置路由的优点在于实现简单，扩充表的时候只需要指定迁移的数据，修改路由表就行了，缺点在于必须多查询一次路由表，而且路由表数据量太大的话又会成为一个瓶颈</p>

<ul>
<li>查询操作</li>
</ul>


<p>在水平分表之后，一旦涉及到查询全部表数据的SQL（比如多表查询join/查询总数count/全数据order by），都需要进行多次查询然后将结果汇集到一起，无法避免地需要在原来的基础上重写大量业务读写逻辑的代码</p>

<h4>分区</h4>

<p>为了解决单表数据量过大的另外的一种方式，不是把一张表在逻辑上拆分，而是把一张表的数据在物理上分成N个区块，在逻辑上看最终只是一张表，但底层是由N个物理区块组成的，通过将不同数据按一定规则放到不同的区块中提升表的查询效率</p>

<p>比如在MySQL中，在创建表时可以指定分区表的类型以及按哪个字段进行分区，完成后对业务是透明的，原来的SQL也能基本照常使用（可能需要针对分区表做优化，条件中要带上分区条件的列，从而使查询定位到少量的分区上，否则就会扫描全部分区），在MySQL内部会负责路由到具体哪个分区</p>

<p>但分区的缺点在于：分区表中无法使用外键、分区键必须是主键或唯一索引、分区数量有限制</p>

<h2>NOSQL</h2>

<p>关系型数据库存在以下缺点：</p>

<ul>
<li>只能存储行，无法存储数据结构。比如查一个用户列表必须在数据库里拆成多行，查询出来再拼装，不能直接存储一个列表</li>
<li>表结构无法扩展。不存在的列无法进行操作，修改时也会长时间锁表</li>
<li>全文检索功能弱。只能用like进行全表扫描，性能很低</li>
</ul>


<h4>K-V存储</h4>

<p>全称Key-Value存储，典型代表：Redis。是一个高性能K-V缓存系统，Value是具体的数据结构，包括string、hash、set、sort set、list、bitmap、hyperloglog</p>

<p>所以，在Redis里就可以<strong>存储数据结构</strong>了，并且修改操作也很容易，比如LPOP操作时移除并返回key对应的list的第一个元素，如果用关系型数据库来存储，则需要：</p>

<ul>
<li>每条数据必须有编号ID和位置编号</li>
<li>查询出第一条数据</li>
<li>删除第一条数据</li>
<li>更新从第二条开始的所有数据的位置编号</li>
</ul>


<p>可以看出来需要多次SQL操作，性能很低。</p>

<p>Redis的缺点主要是<strong>不支持ACID事务</strong>，Redis的事务只能支持IC（隔离性、一致性）无法保证AD（原子性、持久性），但仍然是高性能缓存的首选</p>

<h4>文档数据库</h4>

<p>为了解决表结构无法扩展而生，大部分存储的格式是JSON，所以好处是：</p>

<ol>
<li><strong>新增字段简单</strong>。业务上新增字段，不需要再修改表结构，直接读写就行</li>
<li><strong>历史数据不会出错</strong>。对于历史数据，不需要新增字段也不会出错，只会返回空值</li>
</ol>


<p>所以根据这个特点，特别适合电商和游戏这类的业务场景，因为不同商品的属性差异非常大，即时是同类商品也有不同的属性</p>

<p>但缺点是<strong>不支持事务、无法实现关系数据库的join操作</strong>，只能用多次查询代替关系数据库的多表联合查询</p>

<h4>列式数据库</h4>

<p>列式数据库就是按列存储的数据库，对应的关系数据库就被称为行数据库，有以下的优势：</p>

<ul>
<li>同时读取多个列的时候效率高，因为这些列式是按行存储在一起的，一次磁盘操作就能把一行数据的各个列都读取到内存</li>
<li>能够一次性完成对一个行中多个列的写操作，保证写操作的原子性和一致性</li>
</ul>


<p>列式数据库的优势：</p>

<ol>
<li><strong>特定场景下节省IO</strong>。比如计算某个城市体重超重的人员数据，列数据库只需要读取每个人的体重这一列就行，但行数据库必须把所有数据都读取出来。所以列式数据库应用在离线的大数据分析和统计场景中，因为主要是对部分单列进行操作，且数据写入后不需要更新删除</li>
<li><strong>更高的存储压缩比</strong>。行数据库压缩率在3：1到5：1，而列数据库在8：1到30：1，因为单个列的数据相似度比行来说更高</li>
</ol>


<p>劣势：<strong>更新多个列性能差</strong>，因为列式存储是将不同的列存储到磁盘上不连续的空间，导致更新多个列的时候磁盘是随机写操作</p>

<h4>全文搜索引擎</h4>

<p>针对的是第三个关系型数据库的缺点，解决全文快速查询，关系型数据库不可能对每个字段建立索引，模糊匹配也是全表扫描，性能很差</p>

<p>基本原理是<strong>倒排索引</strong>，建立单词到文档的索引。比如有两篇文章1和2<br/>
文章1的内容为：Tom lives in Guangzhou,I live in Guangzhou too.<br/>
文章2的内容为：He once lived in Shanghai.</p>

<table>
<thead>
<tr>
<th>关键词</th>
<th>文章号</th>
<th>出现频率</th>
<th>出现位置</th>
</tr>
</thead>
<tbody>
<tr>
<td>guangzhou</td>
<td>1</td>
<td>2</td>
<td>3,6</td>
</tr>
<tr>
<td>live</td>
<td>1</td>
<td>2</td>
<td>2,5</td>
</tr>
<tr>
<td>live</td>
<td>2</td>
<td>1</td>
<td>2</td>
</tr>
<tr>
<td>shanghai</td>
<td>2</td>
<td>1</td>
<td>3</td>
</tr>
<tr>
<td>tom</td>
<td>1</td>
<td>1</td>
<td>1</td>
</tr>
</tbody>
</table>


<p>在Lucene中，将上面三列中的所有字段作为基础数据，然后分析出以上的数据结构，最后作为词典文件、频率文件、位置文件保存。其中词典文件不仅保存有每个关键词，还保留了指向频率文件和位置文件的指针，通过指针可以找到该关键字的频率信息和位置信息</p>

<p>比如我想查shanghai，那么先查到文章号2，然后找到文章具体位置，随后展示给我了</p>

<h2>参考</h2>

<p><a href="https://time.geekbang.org/column/intro/81">https://time.geekbang.org/column/intro/81</a><br/>
<a href="https://www.jianshu.com/p/52843a98acda">https://www.jianshu.com/p/52843a98acda</a><br/>
<a href="https://www.zhihu.com/question/19719997">https://www.zhihu.com/question/19719997</a></p>

<h2>号外号外</h2>

<p>最近在总结一些针对<strong>Java</strong>面试相关的知识点，感兴趣的朋友可以一起维护~<br/>
地址：<a href="https://github.com/xbox1994/2018-Java-Interview">https://github.com/xbox1994/2018-Java-Interview</a></p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【架构入门 - 高性能篇】集群高性能]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/05/01/jia-gou-ru-men-gao-xing-neng-pian-ji-qun/"/>
    <updated>2019-05-01T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/05/01/jia-gou-ru-men-gao-xing-neng-pian-ji-qun</id>
    <content type="html"><![CDATA[<p>做到集群高性能其实非常简单，三个字【加机器】</p>

<p>但加机器之后带来的是机器之间配合和集群管理的复杂度，对应也会有解决方案</p>

<!-- more -->


<p><img src="/images/blog/2019-05-01_0.jpg" title="&lsquo;image&rsquo;" ></p>

<h2>负载均衡</h2>

<p>任务分配是高性能集群主要的复杂性体现，所以选择一个合适的负载均衡器和算法是必须的</p>

<h3>分类</h3>

<p><strong>DNS</strong></p>

<p>最简单的方式，一般用来实现地理级别的均衡（AWS Route53就是）。比如来自美国的访问就分配给位于美国的服务器，来自中国的访问就分配给位于中国的服务器</p>

<p>优点：</p>

<ul>
<li>简单、成本低：负载均衡交给了DNS服务器做，不需要自己维护设备</li>
<li>提升访问速度：实现了就近访问，提升性能</li>
</ul>


<p>缺点：</p>

<ul>
<li>更新不及时：DNS有一定的缓存时间，一旦修改DNS配置，还是会访问到之前的服务，导致失败，这也不是负载均衡的目的</li>
<li>扩展性差：无法根据业务特点做一些特殊处理和负载均衡策略</li>
</ul>


<p><strong>硬件负载均衡</strong></p>

<p>单独通过硬件设备来实现负载均衡，比如F5、A10</p>

<p>优点：</p>

<ul>
<li>功能强大：支持各个层级的负载均衡、全面的负载均衡算法</li>
<li>性能强大：100万以上的并发</li>
<li>稳定性高：商用负载均衡经过大规模使用表现稳定</li>
<li>安全防护：防火墙、防DDOS功能</li>
</ul>


<p>缺点：<strong>贵</strong></p>

<p><strong>软件负载均衡</strong></p>

<p>单独通过负载均衡软件来实现负载均衡。比如Nginx（7层）、LVS（4层），区别在于协议和灵活性，Nginx支持HTTP、Email协议以及对应逻辑处理，LVS支持TCP，所有TCP协议基础的都能做</p>

<p>优点：</p>

<ul>
<li>简单：维护、部署</li>
<li>便宜：不要钱</li>
<li>灵活：4层和7层的选择、业务扩展的选择</li>
</ul>


<p>缺点：</p>

<ul>
<li>性能一般</li>
<li>功能不如硬件负载均衡</li>
<li>没有防火墙</li>
</ul>


<p><strong>典型负载均衡架构</strong></p>

<p><img src="/images/blog/2019-05-01_1.jpg" title="&lsquo;image&rsquo;" ></p>

<ul>
<li>地理级别：分别部署服务器到广州、北京、上海三个机房，用户访问时，DNS路由到对应机房</li>
<li>集群级别：每个机房都用上F5，将请求路由到上海集群1或2或3/广州集群1或2或3/北京集群1或2或3</li>
<li>机器级别：通过Nginx路由到机器1或2</li>
</ul>


<h3>算法</h3>

<p><strong>轮询</strong></p>

<p>不关心服务器负荷状态，直接按顺序轮流分配到服务器上，只要服务器在运行，就分配，即使服务器负载特别高难以处理请求，这样明显不合理，但优点是简单</p>

<p><strong>加权轮询</strong></p>

<p>负载均衡系统根据配置的权重，将请求路由到对应服务器上，比如某些机器性能高，某些机器性能差，就将高权重分配给性能高的机器</p>

<p><strong>负载最低优先</strong></p>

<p>将请求路由到当前负载最低的服务器上，比如Nginx可以通过HTTP请求数量来判断服务器当前负载，还可以自己写一个根据CPU压力来分配的负载均衡系统，但是系统复杂度上升，甚至需要为负载均衡器单独开发算法并部署然后碰到一堆莫名其妙的问题，实际的应用场景反而没有轮询用的多</p>

<p><strong>性能最优</strong></p>

<p>将请求分配给处理速度最快的服务器，从而达到最快响应速度，但是同样存在判断处理速度的算法问题</p>

<p><strong>Hash</strong></p>

<p>根据请求的IP或者其他信息进行Hash运算，将相同Hash值的请求分配到同一台服务器上，这主要是为了满足业务需求，比如不用分布式Session的集群就可以通过这个方式来实现Session，用户的Session只保存在某台服务器上，后台不需要同步到所有机器或持久化下来；或者AB测试中将某些用户分配到固定的机器上实现粘性会话</p>

<h2>缓存</h2>

<p>除了任务分配、其他中间件的使用也是能够大大提升集群性能的，特别是缓存、消息队列，在特定的场景下有非常惊人的效果</p>

<p>比如在多读少写、不写的情景下，存储系统的写性能没问题，但读性能远远不够。下面是一些常见的缓存架构存在的问题，避免这些问题就能做出一个好的缓存架构</p>

<h3>缓存问题</h3>

<p><strong>缓存穿透</strong></p>

<p>查询一个数据库中不存在的数据，比如商品详情，查询一个不存在的ID，如果此时去查数据库，那么每次都会访问数据库，如果有人恶意破坏，很可能直接对数据库造成过大地压力</p>

<p>当通过某一个key去查询数据的时候，如果对应在数据库中的数据都不存在，我们将此key对应的value设置为一个默认的值。</p>

<p><strong>缓存雪崩</strong></p>

<p>在高并发的环境下，如果此时key对应的缓存失效，此时有多个进程就会去同时去查询数据库，然后再去同时设置缓存。这个时候如果这个key是系统中的热点key或者同时失效的数量比较多时，数据库访问量会瞬间增大，造成过大的压力</p>

<p>将系统中key的缓存失效时间均匀地错开、或者通过分布式锁加锁保护</p>

<p><strong>热点key</strong></p>

<p>缓存中的某些Key(可能对应用与某个促销商品)对应的value存储在集群中一台机器，使得所有流量涌向同一机器，成为系统的瓶颈，该问题的挑战在于它无法通过增加机器容量来解决。</p>

<ol>
<li>客户端热点key缓存：将热点key对应value并缓存在客户端本地，并且设置一个失效时间。</li>
<li>将热点key分散为多个子key，然后存储到缓存集群的不同机器上，这些子key对应的value都和热点key是一样的。</li>
</ol>


<h3>使用策略</h3>

<p><strong>延迟加载</strong></p>

<p>读：当读请求到来时，先从缓存读，如果读不到就从数据库读，读完之后同步到缓存且添加过期时间<br/>
写：当写请求到来时，只写数据库</p>

<p>优点：仅对请求的数据进行一段时间的缓存，没有请求过的数据就不会被缓存，节省缓存空间；节点出现故障并不是致命的，因为可以从数据库中得到<br/>
缺点：缓存数据不是最新的；【缓存击穿】；【缓存雪崩】</p>

<p><strong>直写</strong></p>

<p>读：当读请求到来时，先从缓存读，如果读不到就从数据库读，读完之后同步到缓存且设置为永不过期 <br/>
写：当写请求到来时，先写数据库然后同步到缓存，设置为永不过期</p>

<p>优点：缓存数据是最新的，无需担心缓存击穿、失效问题，编码方便<br/>
缺点：大量数据可能没有被读取的资源浪费；节点故障或重启会导致缓存数据的丢失直到有写操作同步到缓存；每次写入都需要写缓存导致的性能损失</p>

<p>永不过期的缓存会大量占用空间，可以设置过期时间来改进，但是会引进【缓存失效】问题，需要注意解决</p>

<p><strong>如何选择</strong></p>

<p>如果需要缓存与数据库数据保持实时一致，则需要选择直写方式<br/>
如果缓存服务很稳定、缓存的可用空间大、写缓存的性能丢失能够接受，选择直写方式比较方便实现<br/>
否则选择延迟加载，同时注意解决引进的问题</p>

<h3>缓存集群架构</h3>

<p><strong>主从</strong>
<img src="https://github.com/xbox1994/2018-Java-Interview/raw/master/images/redis%E4%B8%BB%E4%BB%8E.png" alt="" /></p>

<p>用一个redis实例作为主机，其余的实例作为从机。主机和从机的数据完全一致，主机支持数据的写入和读取等各项操作，而从机则只支持与主机数据的同步和读取。因而可以将写入数据的命令发送给主机执行，而读取数据的命令发送给不同的从机执行，从而达到读写分离的目的。</p>

<p>问题是主从模式如果所连接的redis实例因为故障下线了，没有提供一定的手段通知客户端另外可连接的客户端地址，因而需要手动更改客户端配置重新连接。如果主节点由于故障下线了，那么从节点因为没有主节点而同步中断，因而需要人工进行故障转移工作。为了解决这两个问题，在2.8版本之后redis正式提供了sentinel（哨兵）架构。
<strong>哨兵</strong>
<img src="https://github.com/xbox1994/2018-Java-Interview/raw/master/images/redis%E5%93%A8%E5%85%B5.png" alt="" /></p>

<p>由Sentinel节点定期监控发现主节点是否出现了故障，当主节点出现故障时，由Redis Sentinel自动完成故障发现和转移，并通知应用方，实现高可用性。</p>

<p><strong>集群</strong>
<img src="https://github.com/xbox1994/2018-Java-Interview/raw/master/images/redis%E9%9B%86%E7%BE%A4.png" alt="" /></p>

<p>redis主从或哨兵模式的每个实例都是全量存储所有数据，浪费内存且有木桶效应。为了最大化利用内存，可以采用集群，就是分布式存储。集群将数据分片存储，每组节点存储一部分数据，从而达到分布式集群的目的。</p>

<p>上图是主从模式与集群模式的区别，redis集群中数据是和槽（slot）挂钩的，其总共定义了16384个槽，所有的数据根据一致哈希算法会被映射到这16384个槽中的某个槽中；另一方面，这16384个槽是按照设置被分配到不同的redis节点上。</p>

<p>但集群模式会直接导致访问数据方式的改变，比如客户端向A节点发送GET命令但该数据在B节点，redis会返回重定向错误给客户端让客户端再次发送请求，这也直接导致了必须在相同节点才能执行的一些高级功能（如Lua、事务、Pipeline）无法使用。另外还会引发数据分配的一致性hash问题可以参看<a href="https://github.com/crossoverJie/JCSprout/blob/master/MD/Consistent-Hash.md">这里</a></p>

<p><strong>如何选择</strong></p>

<ol>
<li>集群的优势在于高可用，将写操作分开到不同的节点，如果写的操作较多且数据量巨大，且不需要高级功能则可能考虑集群</li>
<li>哨兵的优势在于高可用，支持高级功能，且能在读的操作较多的场景下工作，所以在绝大多数场景中是适合的</li>
<li>主从的优势在于支持高级功能，且能在读的操作较多的场景下工作，但无法保证高可用，不建议在数据要求严格的场景下使用</li>
</ol>


<h2>消息队列</h2>

<p>缓存是在高并发读的场景下很有用，消息队列则是在需要异步交互的系统中发挥到作用，比如：</p>

<p><strong>优点</strong></p>

<ol>
<li>减少请求响应时间。比如注册功能需要调用第三方接口来发短信，如果等待第三方响应可能会需要很多时间</li>
<li>服务之间解耦。主服务只关心核心的流程，其他不重要的、耗费时间流程是否如何处理完成不需要知道，只通知即可</li>
<li>流量削锋。对于不需要实时处理的请求来说，当并发量特别大的时候，可以先在消息队列中作缓存，然后陆续发送给对应的服务去处理</li>
</ol>


<p><strong>缺点</strong></p>

<ol>
<li>系统可用性降低。系统引入的外部依赖越多，越容易挂掉。</li>
<li>系统复杂度提高。保证消息没有重复消费？处理消息丢失的情况？保证消息传递的顺序性？</li>
</ol>


<p><strong>消息重复消费问题</strong></p>

<p>消费端处理消息的业务逻辑保持幂等性，在消费端实现</p>

<p>或</p>

<p>利用一张日志表来记录已经处理成功的消息的ID，如果新到的消息ID已经在日志表中，那么就不再处理这条消息，在消息系统实现</p>

<p><strong>消息丢失问题</strong></p>

<p>生产者弄丢了数据？</p>

<p>生产者将数据发送到 RabbitMQ 的时候，可能数据就在半路给搞丢了<br/>
RabbitMQ 提供的事务功能，就是生产者发送数据之前开启 RabbitMQ 事务。然后发送消息，如果消息没有成功被 RabbitMQ 接收到，那么生产者会收到异常报错。但吞吐量会下来，因为太耗性能<br/>
可以开启confirm模式，你每次写的消息都会分配一个唯一的 id，然后如果写入了 RabbitMQ 中，RabbitMQ 会给你回传一个ack消息，告诉你说这个消息 ok 了。事务机制是同步的，但confirm机制是异步的，发送个消息之后就可以发送下一个消息，RabbitMQ 接收了之后会异步回调你一个接口通知你这个消息接收到了。所以用confirm机制</p>

<p>RabbitMQ 弄丢了数据？</p>

<p>RabbitMQ 自己挂掉导致数据丢失<br/>
开启 RabbitMQ 的持久化，消息写入之后会持久化到磁盘，哪怕是 RabbitMQ 自己挂了，恢复之后会自动读取之前存储的数据</p>

<p>消费端弄丢了数据？</p>

<p>RabbitMQ 如果丢失了数据，主要是因为你消费的时候，刚消费到，还没处理，结果进程挂了，比如重启了，RabbitMQ 认为你都消费了，这数据就丢了<br/>
关闭 RabbitMQ 的自动ack，可以通过一个 api 来调用就行，然后每次你自己代码里确保处理完的时候，再在程序里ack一把。RabbitMQ 就认为你还没处理完，这个时候 RabbitMQ 会把这个消费分配给别的 consumer 去处理，消息是不会丢的</p>

<p><strong>消息顺序性</strong></p>

<p>消息有序指的是可以按照消息的发送顺序来消费。例如：一笔订单产生了 3 条消息，分别是订单创建、订单付款、订单完成。消费时，要按照顺序依次消费才有意义<br/>
消息体通过hash分派到队列里，每个队列对应唯一一个消费者。比如下面的示例中，订单号相同的消息会被先后发送到同一个队列中：</p>

<p><img src="https://github.com/xbox1994/2018-Java-Interview/raw/master/images/%E6%B6%88%E6%81%AF%E9%A1%BA%E5%BA%8F%E6%80%A7.jpg" alt="" /></p>

<p>在获取到路由信息以后，会根据MessageQueueSelector实现的算法来选择一个队列，同一个订单号获取到的肯定是同一个队列</p>

<h3>消息集群模式</h3>

<p><strong>普通集群模式</strong></p>

<p>在多台机器上启动多个 RabbitMQ 实例。你创建的 queue，只会放在一个 RabbitMQ 实例上，但是每个实例都同步 queue 的元数据（元数据可以认为是 queue 的一些配置信息，通过元数据，可以找到 queue 所在实例）。你消费的时候，实际上如果连接到了另外一个实例，那么那个实例会从 queue 所在实例上拉取数据过来。</p>

<p>缺点是不能保证高可用、还有拉去数据的开销、以及单实例的性能瓶颈，所以这个方案是为了提高吞吐量的</p>

<p><strong>镜像集群模式</strong></p>

<p>每个 RabbitMQ 节点都有这个 queue 的一个完整镜像，包含 queue 的全部数据的意思。然后每次你写消息到 queue 的时候，都会自动把消息同步到多个实例的 queue 上。任何一个机器宕机了，没事儿，其它机器（节点）还包含了这个 queue 的完整数据，别的 consumer 都可以到其它节点上去消费数据。</p>

<p>缺点是即时满足了高可用，但因为同步数据量太重导致难以扩展节点，也没有在架构上实现负载均衡，可以参考Redis的集群模式进行优化。</p>

<p><strong>Kafka的高可用性</strong></p>

<p>Kafka 一个最基本的架构认识：为大数据而生的消息系统，由多个 broker 组成，每个 broker 是一个节点；你创建一个 topic，这个 topic 可以划分为多个 partition，每个 partition 可以存在于不同的 broker 上，每个 partition 就放一部分数据，从而可以存储大量数据且难以丢失</p>

<p>每个 partition 的数据都会同步到其它机器上，形成自己的多个 replica 副本。所有 replica 会选举一个 leader 出来，那么生产和消费都跟这个 leader 打交道，然后其他 replica 就是 follower。写的时候，leader 会负责把数据同步到所有 follower 上去，读的时候就直接读 leader 上的数据即可</p>

<p>如果某个 broker 宕机了，该 broker上面的 partition 在其他机器上都有副本的，如果这上面有某个 partition 的 leader，那么此时会从 follower 中重新选举一个新的 leader 出来，大家继续读写那个新的 leader 即可。这就有所谓的高可用性了</p>

<p><img src="https://github.com/xbox1994/2018-Java-Interview/raw/master/images/kafka%E9%9B%86%E7%BE%A4.png" alt="" /></p>

<h2>参考</h2>

<p><a href="https://time.geekbang.org/column/intro/81">https://time.geekbang.org/column/intro/81</a><br/>
<a href="https://github.com/xbox1994/2018-Java-Interview/blob/master/MD/%E6%95%B0%E6%8D%AE%E5%BA%93-Redis.md">https://github.com/xbox1994/2018-Java-Interview/blob/master/MD/%E6%95%B0%E6%8D%AE%E5%BA%93-Redis.md</a></p>

<h2>号外号外</h2>

<p>最近在总结一些针对<strong>Java</strong>面试相关的知识点，感兴趣的朋友可以一起维护~<br/>
地址：<a href="https://github.com/xbox1994/2018-Java-Interview">https://github.com/xbox1994/2018-Java-Interview</a></p>
]]></content>
  </entry>
  
</feed>
