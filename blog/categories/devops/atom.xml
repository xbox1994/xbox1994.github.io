<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Devops | 干掉这里的Bug]]></title>
  <link href="http://www.wangtianyi.top/blog/categories/devops/atom.xml" rel="self"/>
  <link href="http://www.wangtianyi.top/"/>
  <updated>2021-06-26T14:40:14+08:00</updated>
  <id>http://www.wangtianyi.top/</id>
  <author>
    <name><![CDATA[Tianyi Wang]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[【研发效能之道】个人效能实践]]></title>
    <link href="http://www.wangtianyi.top/blog/2020/02/19/yan-fa-xiao-neng-zhi-dao--ge-ren-xiao-neng-shi-jian/"/>
    <updated>2020-02-19T18:58:15+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2020/02/19/yan-fa-xiao-neng-zhi-dao--ge-ren-xiao-neng-shi-jian</id>
    <content type="html"><![CDATA[<p>程序员个体如何高效地进行开发工作</p>

<!-- more -->


<h2>Facebook的10x程序员效率心法</h2>

<h3>第一条原则：抽象和分而治之</h3>

<p>把一个系统拆分为几个有限的子系统，每个子系统涵盖某一方面的内容，并将其复杂性隐藏起来，只对外暴露关键信息。所以，拿到一个任务之后，我们要做的首先就是进行模块的定义，也就是抽象，然后对其分而治之。</p>

<p>比如一个功能由一个前端开发者和两个后端开发者完成，整个研发过程至少涉及 3 个抽象和分而治之的操作：</p>

<ol>
<li>前后端模块进行自然的拆分。这时，前后端开发者一定会一块儿认真讨论，明确前后端代码运行时的流程，后端需要提供的 API，以及交付这些 API 的时间。</li>
<li>两个后端开发者对后端工作进行拆分，确定各自的工作任务和边界。</li>
<li>每个开发者对自己负责的部分再进行抽象和拆分。</li>
</ol>


<p>在这个过程中，一定要明确模块之间的依赖关系，尽快确定接口规格和可调用性。比如，在前后端的拆分中，常常会采用这几个步骤处理 API：</p>

<ol>
<li>前后端开发者一起讨论，明确需要的 API。</li>
<li>后端人员会先实现 API 的 Mock，返回符合格式规范的数据。在这个过程中，后端开发者会尽快发出代码审查的要求给另一个后端和前端开发者，以确保格式正确。</li>
<li>Mock 实现之后尽快推到主仓的 master 上 (也就是 origin/master)，并尽快将其部署到内部测试环境，让前端开发者可以使用内部测试环境进行开发和调试。</li>
<li>这些 API 还不能面对用户，通常会先使用功能开关让它只对公司开发人员可见。这样的话，即使 API 的代码在 origin/master 上部署到了生产环境，也不会对用户产生影响。</li>
</ol>


<h3>第二条原则：快速迭代</h3>

<ol>
<li>不要追求完美，不要过度计划，而是要尽快实现功能，通过不断迭代来完善</li>
<li>在设计的实现中，尽量让自己的代码能够尽快运行起来，从而尽快地验证结果</li>
<li>为了能够快速进行验证，一个重要实践是设置好本地的代码检验</li>
<li>代码写好之后，尽快提交到主代码仓并保证不会阻塞其他开发人员</li>
</ol>


<p>这是代码提交的<strong>原子性</strong>，可以保证主代码仓在理论上能够随时基于 master 分支上的任何提交，构建出可以运行的、直接面对用户的产品。在这种方式下，每个开发者在任何时候都可以基于 origin/master 进行开发，从而确保 Facebook 几千人共主干开发时分而治之能够顺利进行。</p>

<h3>第三条原则：DRY</h3>

<p>不要重复你自己</p>

<ol>
<li>代码逻辑的重复，不仅仅是工作量的浪费，还会大大降低代码的质量和可维护性。所以，我们在开发时，需要留意重复的代码逻辑，并进行适当的处理。</li>
<li>流程的重复。比如测试中，我们常常需要重复地产生一些测试数据，运行完测试之后再把这些数据删除。最主要的办法是自动化。以重复的测试数据产生、删除流程为例，一般的做法是，编写脚本进行自动化，当然有些时候也需要写一些复杂的可执行程序来生成数据。</li>
</ol>


<p><img src="/images/blog/2020-02-19_1.jpeg" title="&lsquo;image&rsquo;" ></p>

<h2>深度工作：聚焦最有价值的事儿</h2>

<p>在任务多、干扰多的现状下，如何最高效地利用时间，去做最重要的事儿，同时有更多的时间来放松和享受生活。</p>

<p>深度工作这个概念，是由卡尔 · 纽波特（Cal Newport）提出的，指的是在无干扰的状态下，才能专注地进行的专业活动。这样的活动能够使个人的认知能力发挥到极限，从而让我们创造出最大价值，成为一个不可替代的人。</p>

<h3>以终为始，寻找并聚焦最重要的任务</h3>

<p><strong>第一步，自己定义任务</strong></p>

<p>预先计划好的任务的优先级常常会随着情况的改变而改变，而临时产生的任务很可能当时觉得很紧急，但实际上是可以推迟或者甚至不做的。我们根据当前情况决定要做的任务，就可以避免把时间花在低优先级的任务上。</p>

<p><strong>第二步，聚焦目标，以终为始</strong></p>

<p>其实就是在自己定义任务。因为时间有限，为目标服务的任务才最重要。常见的目标包括业务成功、帮助团队，以及个人成长这 3 个。如果能找到三者重合的任务就最好不过了。</p>

<p><strong>第三步，无情的筛选，少即是多</strong></p>

<p>“数字 3 原则”很有效，也就是强制把要做的事、要达到的目标，都限制在 3 个以内。</p>

<h3>追根究底，寻找最高效的解决方案</h3>

<p>产品经理决定如何在业务上满足用户需求，而开发人员就是，从产品经理手中接过任务，马上开始技术上的设计和实现。</p>

<p>这种工作方法我们很熟悉，但其实有很大的局限性。因为开发人员对技术实现最熟悉，如果我们能结合业务进行思考，追根究底，常常能对业务的解决方案做出一些改进，甚至是重新设计出更好的方案。而这样的改进，对个人研发效能的提高帮助非常大。</p>

<p>所以，开发人员也要对业务有一定的了解。面对任务的时候，多问几个 Why，并与产品经理和团队成员充分沟通，了解它到底要解决什么问题，只有这样，我们才能以解决问题为出发点，找到最高效的解决方案。</p>

<h3>安排时间和精力，高效执行解决方案</h3>

<p><strong>用番茄工作法来记录深度工作时间</strong></p>

<p>番茄工作法就是把时间划分为固定时长的工作时间和休息时间。一个番茄时间包含两部分，25 分钟的工作学习和 5 分钟的休息时间。当然25这个数字我个人想修改为1个小时。</p>

<p>番茄工作法的精髓是，在每一个工作时间段，避免干扰、全神贯注。因为只有精力高度集中，减少上下文切换，才能进入深度工作状态，进而最大程度地发挥我们的心智能力，提高个人效能。</p>

<p><strong>用拥抱无聊来控制手机依赖</strong></p>

<p>不知道你有没有这样的感受，当习惯一有空闲就使用手机之后，在工作中稍有一点空闲，或者是遇到比较难以处理的问题时，就会不自觉地想停下来看一看手机。我认为，这就是因为我们习惯了用手机来缓解紧张和压力而形成的条件反射，就是对手机的一种依赖。</p>

<p>发现了一个“拥抱无聊”的方法。具体来说就是，在一些非常碎片化的时间，不要因为无聊就马上抄起手机，去找一些有意思的东西放松或者学习，而是尝试适应无聊。无聊也是一种不错的状态，试着去享受它，不要让自己的大脑一直处于活跃状态。这样可以让我们得到休息，而且更重要的是，能够比较好地避免自己打扰自己的倾向。</p>

<p><strong>用反向行事日历来确保休息和高效工作</strong></p>

<p>我们在安排日程时，通常会把工作任务放到日历中，而不会把运动、休息、吃饭等非工作任务放入其中。反向日历正好相反，首先就是把这些非工作任务作为最为最高优先级放到日历中，然后再在剩余的时间中安排工作任务。</p>

<p>好处：第一，可以强制我们去休息和锻炼；第二，可以让我们更有紧迫感，提高工作效率</p>

<h2>开发能力</h2>

<p>个人效能影响最大的方面其实是自身的开发能力，如何提升开发能力至关重要但不在本文范围内</p>

<p>另外，效率工具如git/vim/命令行/ide都有自己最适合的组合，也是提升个人效能的一部分，在开发的过程中需要记得持续优化改善</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【研发效能之道】研发流程优化实践]]></title>
    <link href="http://www.wangtianyi.top/blog/2020/02/18/yan-fa-xiao-neng-zhi-dao--yan-fa-liu-cheng-you-hua-shi-jian/"/>
    <updated>2020-02-18T15:21:28+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2020/02/18/yan-fa-xiao-neng-zhi-dao--yan-fa-liu-cheng-you-hua-shi-jian</id>
    <content type="html"><![CDATA[<p>接下来会以<strong>提高用户价值的流动效率</strong>为核心，列出一些具体的研发流程优化实践</p>

<!-- more -->


<h2>代码入库前</h2>

<p><img src="/images/blog/2020-02-18_1.png" title="&lsquo;image&rsquo;" ></p>

<p>代码入库之前的开发活动，主要包括编码、调测调优、静态检查、自动化测试、代码审查等。这是开发者编写代码的步骤，自然是提高研发效能的关键环节。</p>

<p>代码集成越晚发现问题就越晚。这正是产品上线的最后关头合并混乱，产品质量差、返工率高的一个重要原因。</p>

<h3>规范化、自动化核心步骤</h3>

<p><img src="/images/blog/2020-02-18_2.png" title="&lsquo;image&rsquo;" ></p>

<ol>
<li>获取开发环境，包括获取开发机器、配置环境、获取代码等。</li>
<li>在本地开发机器上进行开发，包括本地的编码、调测、单元测试等。</li>
<li>代码入库前，把改动提交到检查中心（比如 Gerrit），再进行一轮系统检查，主要包括代码检查、单元测试、代码审查等，通过之后再入库。</li>
</ol>


<p>对应的有三个实践：</p>

<ol>
<li>提高开发环境的获取效率。把整个开发环境的获取，进行服务化、自助化。</li>
<li>规范化、自动化化本地检查。根据团队实际情况，找到合适的工具和配置进行这些检查，并让团队成员统一使用。</li>
<li>建设自动化代码入库前的检查流程。比如merge request代码审查、使用 GitLab 提供的 GitLab CI/CD 框架自动化运行各种代码检查工具</li>
</ol>


<h3>提供快速反馈，促进增量开发</h3>

<p>提供快速反馈，进行增量开发指的是，能够快速验证已经完成的开发工作，说白了就是边开发边验证。</p>

<ol>
<li>灵活使用各种 Linter 和测试。最常用的快速验证方法就是，提高运行静态检查和测试的方便性、灵活性。各种语言、框架都有自己的测试框架和 Linter，根据语言自行设置</li>
<li>使用实时检验工具。最常见的是，IDE 中的实时语法检查。我们可以花一些时间来配置 IDE。另外，有些工具可以自动监视文件系统的变化，文件有变化时自动重启服务。这对于开发者来说，非常便利。</li>
</ol>


<h2>代码入库到产品上线</h2>

<h3>三个持续</h3>

<p><strong>持续集成：在团队协作中，一天内多次将所有开发人员的代码合并入同一条主干，推荐Trunk Based主干分支模型</strong></p>

<ul>
<li>它能够帮助开发人员尽量早、尽量频繁地把自己的改动推送到共享的代码仓库分支上，进行代码集成，从而减少大量代码冲突造成的低效能问题</li>
<li>更快地服务客户，以更快的试错速度寻找到用户，提供真正对客户有价值的功能</li>
<li>产品发布到不同环境的过程中，我们会提前发现一些在开发和持续集成中没有暴露的问题</li>
</ul>


<p>问题：如果有跨多个发布周期开发的功能，有两种处理方式，一是功能开关，但会引进更多逻辑，二是放弃持续集成，那就需要开一个新的功能分支开发（功能分支模型）。所以团队中使用哪种分支模型需要按下图进行判断：</p>

<p><img src="/images/blog/2020-02-18_4.jpeg" title="&lsquo;image&rsquo;" ></p>

<p><strong>持续交付：一种软件工程方法，在短周期内完成软件产品，以保证软件保持在随时可以发布的状态</strong></p>

<p>目标是，对每一个进入主干分支的代码提交，构建打包成为可以发布的产品。对每一个提交，把集成后的代码部署到“类生产环境”中进行验证。如果代码没有问题，后续可以手动部署到生产环境中。</p>

<p><strong>持续部署：将每一个代码提交，都构建出产品直接部署给用户使用</strong></p>

<p>值得一提的是，跟持续交付一样，Facebook 的持续部署也不是纯粹的持续部署。因为代码提交太多，他们并没有每个提交都单独部署，而是采用类似持续交付的方法，把一段时间之内的提交一起部署。这种不教条的方式，是 Facebook 到的一个重要的做事方法</p>

<p><img src="/images/blog/2020-02-18_3.jpeg" title="&lsquo;image&rsquo;" ></p>

<h3>三个原则</h3>

<p><strong>基本原则 1：流水线的测试要尽量完整</strong></p>

<p>CI/CD 流水线的测试只有尽量完整，代码和产品的质量才能有保证。所以，最主要的工程实践，就是在流水线中运行大量高质量的测试和检查。</p>

<p><strong>基本原则 2：流水线的运行速度一定要快</strong></p>

<ul>
<li>使用并行方式运行各种测试来提速</li>
<li>投入硬件资源，使用水平扩展的方式来提速</li>
<li>使用增量测试的方式进行精准验证。也就是说，只运行跟当前改动最相关的测试，以减少测试用例的运行数量</li>
<li>让开发人员在本地也能运行这些测试，从而使用本地资源尽早发现问题</li>
<li>运行测试的时候，按照一定的顺序运行测试用例。比如可以先运行速度快的用例，以及历史上容易发现问题的用例</li>
</ul>


<p><strong>基本原则 3：流水线使用的环境，尽量和生产环境一致</strong></p>

<ul>
<li>软件包最好只构建一次，保证各种不同环境都用同一个包</li>
<li>使用 Docker 镜像的方式，把发布的产品以及环境都打包进去，实现环境的一致性</li>
<li>尽量使用干净的环境。比如，测试时，使用刚从镜像产生的系统；又比如，使用蓝绿部署，每次产生新的部署时，直接丢弃旧的环境。</li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【研发效能之道】研发流程优化理论]]></title>
    <link href="http://www.wangtianyi.top/blog/2020/02/17/yan-fa-xiao-neng-zhi-dao--yan-fa-liu-cheng-you-hua-li-lun/"/>
    <updated>2020-02-17T15:49:59+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2020/02/17/yan-fa-xiao-neng-zhi-dao--yan-fa-liu-cheng-you-hua-li-lun</id>
    <content type="html"><![CDATA[<p>现在再次开始一个新的系列，【研发效能之道】，主要参考《极客时间|研发效率破局之道》，系列内容为其读书笔记与自己的理解、总结</p>

<!-- more -->


<h2>问题</h2>

<ol>
<li>用户需求从需求分析、产品设计、开发、测试最终流到部署，但最终发布的产品与用户需求偏差却很大。</li>
<li>产品发布上线时出现大量提交、合并，导致最后时刻出现很多问题，团队成员集体熬夜加班，却将大把的时间花在了等待环境、等待验证上。</li>
<li>开发提测质量不好，大量压力聚集到测试这一步，导致代码返工率很高。引入单元测试、代码审查，效果却都不明显。</li>
</ol>


<p>这其实就是团队的研发效率问题，从而会让我们思考如何去提升研发效能</p>

<h2>定义</h2>

<p>团队能够持续地为用户产生有效价值的效率。包括：有效性、效率、可持续性。简单来说，就是能否长期、高效地开发出有价值的产品</p>

<p>研发效能的提高，需要整个公司在以下方面精心设计：</p>

<p><img src="/images/blog/2020-02-17_1.png" title="&lsquo;image&rsquo;" ></p>

<ul>
<li>研发流程。提升整个流程的顺畅度，包括单个部分的顺畅以及各个部分之间传递的顺畅，如使用敏捷、持续集成、分支管理、持续交付、DevOps、团队协同方法论和相关实践</li>
<li>工程方法。研发流程中各关键步骤高效工程方法，包括开发、测试、运维。智能化开发、在移动设备上开发、云原生</li>
<li>个人效能。深度工作、Git、命令行、VIM、工具环境集成，提升每个开发的自身开发效率</li>
<li>管理和文化。学习硅谷管理和文化，尤其是 Facebook 的工程师文化，给出推荐的文化引入和建设方法</li>
</ul>


<h2>研发流程</h2>

<p>研发流程的优化涉及到方法论，如瀑布、敏捷、精益、看板。但国内实施的不理想，有个知乎上的前同事回答可参考：</p>

<p><a href="https://www.zhihu.com/question/340916482/answer/796443921">为何谷歌之类大厂程序员认为敏捷开发是瞎扯淡？ - 熊节的回答 - 知乎</a></p>

<p><img src="/images/blog/2020-02-17_2.png" title="&lsquo;image&rsquo;" ></p>

<p>学习方法论的时候建议使用黄金圈法则：最中间的一个圆是 Why，也就是这个方法论的目标，是要解决一个什么问题；第二个圆是 How，也就是这个方法论的基本原则、指导思想；最外层的圆是 What，也就是这个方法论的具体实践。在使用一个方法论的时候，一定要从内往外看。</p>

<ul>
<li>中心的目标一般错不了。比如，敏捷的目标就是快速应对变化。</li>
<li>原则的通用性就差一些，你需要在理解的基础上挑选适合自己的。比如，敏捷中有一条原则是“面对面交谈是最好的沟通方式”，就不一定适合你的团队。</li>
<li>具体的实践，就更要小心，切忌生搬硬套。</li>
</ul>


<p>在引入实践的时候，建议是逐步优化已有的开发流程和框架，甚至只给出原则，让团队成员逐步摸索并最终找到合适的方法。</p>

<p>比如，你可以把一个核心原则通知给团队：“流程中总是有一个核心瓶颈。找到它，并解决它”。通过这样一个基本原则的指导，让具体实践逐步形成，对现有工作的影响，以及受到的阻力都会比较小。</p>

<h3>目标一：寻找用户价值</h3>

<p>以终为始地来看，我们最终目的是产生用户价值，生存下去。为此，我们经常需要主动去寻找最好的产品形态。只有方向找准了，流程产生的结果才能有效，才能产生用户价值。所以，优化研发流程的第一步，就是提高寻找用户价值的效率。</p>

<ul>
<li>第一条原则是：衡量每一个时间段成果的标准，应该是价值假设方面的进展。比如，你的团队在一月份开发了 5 个功能，用户反馈都一般。这时，你看不出用户更喜欢什么东西，更讨厌什么东西。而在二月份，你们只开发了一个功能，预计很有用，但收到的用户反馈却是负面的。二月份能明确告诉你，这个假设是错的，让你们在寻找产品符合市场吻合度（Product-Market-Fit）上前进了一大步。</li>
<li>第二条原则是：使用最小可行性产品（Minium Viable Product，MVP）来帮助学习。这里的关键点是，要以探索价值为出发点设计产品，最快地验证你的假设，功能要尽量少，能够使用就可以。具体的方法有数据驱动、A/B 测试、灰度发布等。</li>
</ul>


<h3>目标二：提高用户价值的流动效率</h3>

<p>软件研发是一条流水线，里面流动的是一个一个给用户提供价值的功能。有四条基本原则：</p>

<ol>
<li>让功能尽快地流动。对开发人员来说，需求需要尽量拆分，每做好一个提交就尽快提交，小步提交持续集成，依赖本地的构建速度、方便的测试环境、自动化持续集成、定位问题的方式</li>
<li>让节点之间的联动更加顺畅；可以通过对关键流程的自动化、工具之间的网状互联，以及节点之间的融合来实现。在具体实践上，个人代码上线后，在和他人的代码集成时容易出现问题，这时就可以使用流水线来自动化代码集成过程</li>
<li>节点之间的融合；也是为了保证节点间的联动顺畅。也就是模糊节点间的边界，让功能在节点之间的流动更顺畅。比如，职能团队提供平台和工具，让全栈工程师能够自己处理端到端的工作。比如，测试团队提供测试平台和工具，运维团队提供运维平台和工具，这些平台和工具可以通过服务化自助使用。</li>
<li>发现整个流程中的瓶颈，并解决它们。</li>
</ol>


<p>具体的实践有<strong>可视化和复盘</strong></p>

<p>在可视化方面，粘上便利贴的白板，或是像 Trello 那样的电子看板就是很好的工具。真正的“看板”是信号卡片，用来表示流水线系统可以增加新的工作项了。通过任务可视化，我们可以直观地看到哪一个环节的任务特别多，卡住了，也就是瓶颈在哪儿</p>

<p>复盘方面，Facebook 做得也很好。他们有一个 SEV 复盘系统，用来记录公司发生的重要事故进行复盘。每个团队也会不定期地复盘，以定位瓶颈问题并提高。敏捷实践中还有retro回顾会议，通过让所有成员提出well/less well/suggstion来回顾并改进存在问题</p>

<h2>小结</h2>

<p>优化流程，是提高研发效能的第一步。我们应该按照目标、原则和具体实践的顺序学习和使用各种方法论，选择和配置最适合自己团队的工程实践。</p>

<p>Facebook 在流程方面的实践是以实用主义的态度，从原则出发，灵活优化流程。没有很多新方法论的时髦术语，但是很多实践却和这些方法论的原则是一致的，甚至是超前这些方法论的</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【Prometheus】Prometheus的设计]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/06/10/prometheus-prometheus-de-she-ji/"/>
    <updated>2019-06-10T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/06/10/prometheus-prometheus-de-she-ji</id>
    <content type="html"><![CDATA[<!-- more -->


<h2>简介</h2>

<p>Prometheus是带时序数据库的开源监控告警系统</p>

<p>Google发起的Linux基金会旗下的原生云基金会（Cloud Native Computing Foundation,CNCF）将Prometheus纳入其第二大开源项目，Kubernetes是第一个</p>

<p>随着 Kubernetes 在容器调度和管理上确定领头羊的地位，Prometheus 也成为 Kubernetes 容器监控的标配</p>

<h3>优点</h3>

<ul>
<li>提供多维度数据模型和灵活的查询方式，通过将监控指标关联多个tag，来将监控数据进行任意维度的组合，并且提供简单的PromQL查询方式，还提供HTTP查询接口，可以很方便地结合Grafana等GUI组件展示数据</li>
<li>支持服务器节点的本地存储，通过Prometheus自带的时序数据库，可以完成每秒千万级的数据存储；在保存大量历史数据的场景中，Prometheus可以对接第三方时序数据库如OpenTSDB</li>
<li>定义了开放指标数据标准，以基于HTTP的Pull方式采集时序数据，只有实现了Prometheus监控数据格式的监控数据才可以被Prometheus采集、汇总，并支持以Push方式向中间网关推送时序列数据</li>
<li>支持通过静态文件配置和动态发现机制发现监控对象，自动完成数据采集。Prometheus目前已经支持Kubernetes、etcd、Consul等多种服务发现机制，可以减少运维人员的手动配置环节，在容器运行环境中尤为重要</li>
<li>易于维护，可以通过二进制文件直接启动，并且提供了容器化部署镜像</li>
<li>支持数据的分区采样和联邦部署，支持大规模集群监控</li>
</ul>


<h3>架构</h3>

<p>基本原理：通过HTTP周期性抓取被监控组件的状态，任意组件只要提供对应的 HTTP 接口并且符合 Prometheus 定义的数据格式，就可以接入Prometheus监控</p>

<p><img src="/images/blog/2019-06-10_1.png" title="&lsquo;image&rsquo;" ></p>

<p>Prometheus获取监控对象的方式：</p>

<ol>
<li>通过配置文件、文本文件等进行静态配置</li>
<li>支持ZooKeeper、Consul、Kubernetes等方式进行动态发现，例如对于Kubernetes的动态发现，Prometheus使用Kubernetes的API查询和监控容器信息的变化，动态更新监控对象</li>
</ol>


<p>重要组件：</p>

<ul>
<li>Jobs/Exporters。监控数据采集Agent，Exporter以Web API的形式对外暴露数据采集接口（”/metrics”）。通过将exporter的信息注册到Prometheus Server,实现监控数据的定时采集</li>
<li>TSDB(Time Series and Spatial-Temporal Database时序时空数据库)。通过一定的规则清理和整理数据，并把得到的结果存储到新的时间序列中。支持本地存储于远端存储</li>
<li>PromQL。可视化地展示收集的数据。Prometheus支持多种方式的图表可视化，例如Grafana、自带的PromDash及自身提供的模版引擎等</li>
<li>AlertManager。是独立于 Prometheus 的一个组件，在触发了预先设置在Prometheus 中的高级规则后，Prometheus 便会推送告警信息到 AlertManager。AlertManager提供了十分灵活的告警方式，可以通过邮件、slack或者钉钉等途径推送</li>
<li>Pushgateway。对于某些场景Prometheus无法直接拉取监控数据，Pushgateway的作用就在于提供了中间代理，例如我们可以在应用程序中定时将监控metrics数据提交到Pushgateway。而Prometheus Server定时从Pushgateway的/metrics接口采集数据。</li>
</ul>


<h2>设计</h2>

<h3>指标</h3>

<p><strong>一、定义</strong></p>

<p>Prometheus的所有监控指标（Metric）被统一定义为：</p>

<p><code>&lt;metric name&gt;{&lt;label name&gt;=&lt;label value&gt;, ...}</code></p>

<ul>
<li>指标名称。用于说明指标的含义，例如http_request_total代表HTTP的请求总数</li>
<li>标签名称。体现指标的维度特征，用于过滤和聚合。它通过标签名和标签值这种键值对的形式，形成多种维度</li>
</ul>


<p><strong>二、指标分类</strong></p>

<ul>
<li>Counter。计数器类型，它的特点是只增不减，例如机器启动时间、HTTP访问量等。Counter 具有很好的不相关性，不会因为机器重启而置0。我们在使用 Counter指标时，通常会结合 rate()方法获取该指标在某个时间段的变化率，例如，“HTTP请求总量”指标就属于典型的Counter指标，通过对它进行rate()操作，可以得出请求的变化率</li>
<li>Gauge。仪表盘，表征指标的实时变化情况，可增可减，例如CPU和内存的使用量、网络 I/O 大小等，大部分监控数据都是 Gauge 类型的</li>
<li>Summary。用于凸显数据的分布状况。如果需要了解某个时间段内请求的响应时间，则通常使用平均响应时间，但这样做无法体现数据的长尾效应</li>
<li>Histogram。反映某个区间内的样本个数，通过{le=&ldquo;上边界&rdquo;}指定这个范围内的样本数</li>
</ul>


<p><strong>三、数据样本</strong></p>

<p>Prometheus采集的数据样本都是以时间序列保存的。每个样本都由三部分组成：指标、样本值、时间戳</p>

<p>样本值（64位浮点数）和时间戳（精确到 ms）的组合代表在这个时间点采集到的监控数值</p>

<p><img src="/images/blog/2019-06-10_2.png" title="&lsquo;image&rsquo;" ></p>

<p>可以将一个指标的样本数据保存到一起，横轴代表时间，纵轴代表指标序列。如图所示的每一行都代表由一个指标组成的时间序列，每个点都代表一个监控数值，这些时序数据首先被保存在内存中，然后被批量刷新到磁盘</p>

<h3>数据采集</h3>

<p><strong>一、采集方式</strong></p>

<p>和采用Push方式采集监控数据不同，Prometheus采用 Pull方式采集监控数据。为了兼容 Push方式，Prometheus 提供了 Pushgateway组件</p>

<p>采用Push方式时，Agent主动上报数据，采用 Pull方式时，监控中心（Master）拉取 Agent的数据</p>

<p>其主要区别在于 Agent和Master的主动、被动关系，如图所示</p>

<p><img src="/images/blog/2019-06-10_3.png" title="&lsquo;image&rsquo;" ></p>

<p>具体区别如下：</p>

<table>
<thead>
<tr>
<th>-</th>
<th> Push </th>
<th> Pull</th>
</tr>
</thead>
<tbody>
<tr>
<td>实时性 </td>
<td> 好。可以即时上报 </td>
<td> 差。只能轮询</td>
</tr>
<tr>
<td>状态保存 </td>
<td> Agent无状态。Agent不保存数据，但Master需要维护Agent状态 </td>
<td> Master无状态。Agent保存数据，Master只负责拉取数据</td>
</tr>
<tr>
<td>可控性 </td>
<td> 低。控制方为Agent，上报策略决定数据结果 </td>
<td> 高。控制方为Master，更加主动地控制采集策略</td>
</tr>
<tr>
<td>耦合度 </td>
<td> 高，每个Agent都需要配置Master地址 </td>
<td> 低，Agent不需要感知Master存在</td>
</tr>
</tbody>
</table>


<p><strong>二、服务发现</strong></p>

<ul>
<li>静态文件配置。适用于有固定的监控环境、IP地址和统一的服务接口的场景，需要在配置中指定采集的目标。但如果服务发生迁移、变更，以及更换地址或者端口，就需要重新修改配置文件并通知Prometheus重新加载配置文件</li>
<li>动态发现。如图：</li>
</ul>


<p><img src="/images/blog/2019-06-10_4.png" title="&lsquo;image&rsquo;" ></p>

<p>Prometheus 会从这些组件中获取监控对象，并汇总在这些组件中获取的数据，从而获取所有监控对象</p>

<p>以Kubernetes为例：</p>

<ol>
<li>需要在 Prometheus 里配置 Kubernetes API 的地址和认证凭据，这样Prometheus就可以连接到Kubernetes的API来获取信息</li>
<li>Prometheus 的服务发现组件会一直监听（watch）Kubernetes 集群的变化，当有新主机或加入集群的时候，会获取新主机的主机名和主机IP，如果是新创建的容器，则可以获取新创建 Pod的名称、命名空间和标签等。相应地，如果删除机器或者容器，则相关事件也会被Prometheus感知，从而更新采集对象列表</li>
</ol>


<p><strong>三、数据采集</strong></p>

<p>在获取被监控的对象后，Prometheus便可以启动数据采集任务了</p>

<p>Prometheus采用统一的Restful API方式获取数据，具体来说是调用HTTP GET请求或metrics数据接口获取监控数据。为了高效地采集数据，Prometheus对每个采集点都启动了一个线程去定时采集数据</p>

<p>在修改了采集的时间间隔后，Prometheus通常通过调用Prometheus的reload接口进行配置更新</p>

<h3>数据处理</h3>

<p>Prometheus 支持数据处理，主要包括 relabel、replace、keep、drop 等操作，提供过滤数据或者修改样本的维度信息等功能。</p>

<p><strong>一、重新定义标签</strong></p>

<p>在需要添加或者替换一个标签时需要重新定义标签</p>

<p>通过 replace 或者 labelmap 的方式可以针对这些内部使用的标签进行重命名或者将多个标签的内容进行组合。</p>

<p><strong>二、标签筛选</strong></p>

<p>Prometheus会从 target中获取所有暴露的数据，但某些数据对 Prometheus是无用的，如果直接保存这些数据，则不仅浪费空间，还会降低系统的吞吐量</p>

<ul>
<li>如果设置了keep 机制。会保留所有匹配标签的数据</li>
<li>如果设置了drop机制。会丢弃匹配标签的数据，从而完成数据过滤</li>
</ul>


<p>除了处理 keep或 drop,Prometheus还支持 Hash的分区采集，通过对 target地址计算 Hash值，然后取模匹配 Prometheus设定的值，便可以过滤该Prometheus负责采集的 target，这也是一种服务端负载均衡的方案，从而扩展 Prometheus 的采集能力。下面是一种通过Hash取模的经典用法：</p>

<p><img src="/images/blog/2019-06-10_5.png" title="&lsquo;image&rsquo;" ></p>

<h3>数据存储</h3>

<p><strong>一、本地存储</strong></p>

<p>Prometheus的本地时间序列数据库以自定义格式在磁盘上存储时间序列数据</p>

<p>其中wal目录记录的是监控数据的WAL；每个block是一个目录，该目录下的chunks用来保存具体的监控数据，meta.json用来记录元数据，index则记录索引。具体内容会在有关存储的章节中详细介绍</p>

<pre><code class="text">./data
├── 01BKGV7JBM69T2G1BGBGM6KB12
│   └── meta.json
├── 01BKGTZQ1SYQJTR4PB43C8PD98
│   ├── chunks
│   │   └── 000001
│   ├── tombstones
│   ├── index
│   └── meta.json
├── 01BKGTZQ1HHWHV8FBJXW1Y3W0K
│   └── meta.json
├── 01BKGV7JC0RY8A6MACW02A2PJD
│   ├── chunks
│   │   └── 000001
│   ├── tombstones
│   ├── index
│   └── meta.json
└── wal
    ├── 00000002
    └── checkpoint.000001
</code></pre>

<p><strong>二、远程存储</strong></p>

<p>为了提高对大量历史数据持久化存储的能力，Prometheus 在1.6版本后支持远程存储，Adapter需要实现Prometheus的read接口和write接口，并且将read和write转化为每种数据库各自的协议</p>

<p><img src="/images/blog/2019-06-10_6.png" title="&lsquo;image&rsquo;" ></p>

<p>在用户查询数据时，Prometheus会通过配置的查询接口发送 HTTP请求，查询开始时间、结束时间及指标属性等，Adapter会返回相应的时序数据；相应地，在用户写入数据时，HTTP请求Adapter的消息体会包含时序数组（样本数据）。</p>

<h3>数据查询</h3>

<p>Prometheus提供了一种名为PromQL（Prometheus查询语言）的函数查询语言，用户可以实时选择和汇总时间序列数据</p>

<p>这里先介绍PromQL的使用方式。例如，在查询主机CPU的使用率时可以使用：</p>

<p><code>100 * （ 1 - avg (irate(node_cpu(mode='idle')[5m])) by(job) )</code></p>

<p>可以看到，PromQL不仅可以做指标运算，还支持各种如 sum、avg等的函数。Prometheus 通过解析引擎将查询语句转化为 QUERY 请求，然后通过时序数据库找到具体的数据块，在数据返回后再通过支持内置的函数处理数据，最终将结果返回到前端</p>

<p><img src="/images/blog/2019-06-10_7.png" title="&lsquo;image&rsquo;" ></p>

<p>Prometheus支持Grafana等开源显示面板，通过自定义PromQL可以制作丰富的监控视图。Prometheus本身也提供了一个简单的 Web查询控制台，如图2-14所示，Web控制台包含三个主要模块：Graph指标查询，Alerts告警查询、Status状态查询</p>

<h3>告警</h3>

<p>Prometheus可以根据采集的数据设定告警规则，例如针对 HTTP请求延迟设置的告警规则如下：</p>

<p><code>request_latency_seconds:mean5m(job="myjob") &gt; 0.5</code></p>

<p>Prometheus 通过预先定义的 global.evaluation_interval 定时执行这些 PromQL，如果查询的结果符合上面的告警规则，则会产生一条告警记录，但不会立即发出这条告警记录，而需要经过一个评估时间，如果在评估时间段内每个周期（Prometheus 启动时设置）都触发了该告警规则，则会向外发出这条告警</p>

<p>Prometheus 本身对不会对告警进行处理，需要借助另一个组件 AlertManager，主要功能：</p>

<ul>
<li>告警分组。将多条告警合并到一起发送</li>
<li>告警抑制。当告警已经发出时，停止发送由此告警触发的其他错误告警</li>
<li>告警静默。在一个时间段内不发出重复的告警</li>
</ul>


<h3>集群</h3>

<p><strong>一、联邦</strong></p>

<p>多个 Prometheus节点组成两层联邦结构，如图所示。上面一层是联邦节点，负责定时从下面的Prometheus节点获取数据并汇总，部署多个联邦节点是为了实现高可用；下层的 Prometheus 节点又分别负责不同区域的数据采集，在多机房的事件部署中，下层的每个Prometheus节点都可以被部署到单独的一个机房，充当代理。</p>

<p><img src="/images/blog/2019-06-10_8.png" title="&lsquo;image&rsquo;" ></p>

<p>这种架构不仅降低了单个Prometheus的采集负载，而且通过联邦节点汇聚核心数据，也降低了本地存储的压力。为了避免下层Prometheus的单点故障，也可以部署多套 Prometheus 节点，只是在效率上会差很多，每个监控对象都会被重复采集，数据会被重复保存</p>

<p><strong>二、Thanos</strong></p>

<p>针对Prometheus这些不足，Improbable开源了他们的Prometheus高可用解决方案 Thanos。Thanos和 Prometheus无缝集成，并为 Prometheus带来了全局视图和不受限制的历史数据存储能力</p>

<h2>Prometheus并非监控银弹</h2>

<ul>
<li>Prometheus只针对性能和可用性监控，并不具备日志监控等功能，并不能通过Prometheus解决所有监控问题</li>
<li>由于对监控数据的查询通常都是最近几天的，所以 Prometheus 的本地存储的设计初衷只是存储短期（一个月）数据，并非存储大量历史数据</li>
<li>Prometheus的监控数据没有对单位进行定义，这里需要使用者自己区分或者事先定义好所有监控数据单位，避免发生数据缺少单位的情况</li>
</ul>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[【Prometheus】监控系统概述]]></title>
    <link href="http://www.wangtianyi.top/blog/2019/06/08/prometheus-jian-kong-xi-tong-gai-shu/"/>
    <updated>2019-06-08T16:06:06+08:00</updated>
    <id>http://www.wangtianyi.top/blog/2019/06/08/prometheus-jian-kong-xi-tong-gai-shu</id>
    <content type="html"><![CDATA[<p>现在开始一个新的系列，【Prometheus】，主要参考《深入浅出Prometheus》，基本为其读书笔记加上部分自己的理解</p>

<!-- more -->


<h2>概览</h2>

<p>在本系列中，监控系统特指对数据中心的监控，包括硬件和软件的监控和告警</p>

<p>监控系统的作用：</p>

<ol>
<li>实时监控。提供硬件和软件的运行状态展示</li>
<li>告警。符合预设告警阈值则通过多种方式发送告警信息</li>
<li>辅助决策。大数据监控不仅提供实时状态展现，更能帮助故障回溯和预测风险</li>
</ol>


<p>根据程序设计的角度来分类，监控的种类如下，每种监控都涉及不同的监控指标，不同的收集方式，具体在下文分析</p>

<p><img src="/images/blog/2019-06-08_1.png" title="&lsquo;image&rsquo;" ></p>

<h2>基础资源监控</h2>

<h3>网络</h3>

<ol>
<li>网络性能监控。涉及网络监测、网络实时流量监控（网络延迟、访问量、成功率等）和历史数据统计、汇总和历史数据分析</li>
<li>网络攻击检查。针对内网或者外网的网络攻击如 DDoS 攻击等，通过分析异常流量来确定网络攻击行为</li>
<li>设备监控。对数据中心的网络设备监控，如路由器、防火墙、交换机，通过SNMP协议收集</li>
</ol>


<h3>服务器</h3>

<p>监控分类：</p>

<ol>
<li>主机监控。服务器硬件来源于不同厂商，需要获取不同厂商的服务器硬件信息</li>
<li>虚拟机监控。不同的操作系统如 Windows、Linux，采集软件需要做到跨平台运行以获取对应的指标</li>
<li>容器监控。兼容各种虚拟化环境如虚拟机（KVM、VMware、Xen）及容器（Docker、rkt）</li>
</ol>


<p>采集方式有两种：</p>

<ol>
<li>主机监控。IPMI（Intelligent Platform Management Interface，智能平台管理接口）实现。IPMI是一种标准开放的硬件管理接口，不依赖于操作系统，可以提供服务器的风扇、温度、电压等信息</li>
<li>虚拟机监控。一是内置客户端。在每台机器上安装采集客户端。二是在虚拟机环境下通过Xen API、VMware Vcenter API获取监控数据</li>
<li>容器监控。Docker API或针对不同服务/中间件开发的采集器exporter作为一个运行容器进行导出</li>
</ol>


<p>采集指标：</p>

<ol>
<li>CPU。CPU使用量、用户态百分比、内核态百分比、每个CPU的使用量、等待队列长度、I/O等待百分比、CPU消耗最多的进程、上下文切换次数、缓存命中率</li>
<li>内存。使用量、剩余量、内存占用最高的进程、交换分区大小、缺页异常数</li>
<li>网络I/O。涉及每个网卡的上行流量、下行流量、网络延迟、丢包率</li>
<li>磁盘I/O。涉及硬盘的读写速率、IOPS、磁盘用量、读写延迟</li>
</ol>


<h2>中间件监控</h2>

<p>中间件监控需要针对不同中间件的特点和属性分别监控，目前并没有统一的标准和规范</p>

<p>例如，针对 Kafka 的性能监控通常需要采集 Kafka 集群的 Topic 个数、Broker 数据分区数量、日志 Offset值和生产消费流量等指标</p>

<p>针对 Redis的性能监控通常需要采集 Redis 的内存使用量、连接数和缓存命中率等指标</p>

<p>解决方案通常是分别开发一个数据收集 Agent，该 Agent 将采集中间件的性能指标并将其统一转化成 JSON、文本或者其他监控系统能识别的数据格式，然后汇总到监控中心</p>

<p>Prometheus针对不同的中间件开发了对应的监控代理，例如Kafka exporter、MySQL server exporter、Apache exporter、Redis exporter等exporter，它们负责采集这些中间件的特定指标，并提供HTTP查询接口</p>

<h2>应用程序监控（APM）</h2>

<p>采集的方法是将采集器运行在容器内，导出所有容器运行的数据</p>

<ol>
<li>调用链跟踪。从用户发送请求到后端API服务，以及API服务和关联的中间件或者其他组件之间的调用，构建出一个完整的调用拓扑结构</li>
<li>性能监控。监控组件内部方法的调用层次（Controller→Service→DAO），获取每个函数的执行耗时，从而为性能调优提供数据支撑。还能截获TCP、HTTP网络请求，从而获得执行耗时最长的方法和SQL语句、延迟最大的API等信息</li>
<li>运行状态监控。根据业务指标、日志指标、调用链指标、应用环境指标、应用集群指标、服务组件指标、线程栈和客户端体验数据等，形成全维度的指标，展示为服务运行状态</li>
</ol>


<h2>日志监控</h2>

<p>日志监控采集日志数据（文本类型），并将这些数据汇总到日志存储和搜索引擎中，提供日志检索的 Web 接入。指标监控的对象通常都是数字，而日志监控的对象是文本数据，这就要求存储系统具备文本检索功能</p>

<p>下面是流行的日志监控黄金组合：</p>

<p><img src="/images/blog/2019-06-08_2.png" title="&lsquo;image&rsquo;" ></p>

<ol>
<li>Fluentd：日志采集，还有Filebeat、Flume、Fluent Bit，也有一些应用集成Log4g等日志组件直接输出日志</li>
<li>Kafka：数据整流合并，避免突发日志流量直接冲击Logstash</li>
<li>Logstash：日志整理，完成日志过滤、日志修改等功能</li>
<li>Elasticsearch：日志存储和日志检索，自带分布式存储，可以将采集的日志进行分片存储</li>
<li>Kibana：日志查询组件，负责日志展现，主要通过Elasticsearch的HTTP接口展现日志</li>
</ol>


<h2>监控系统实现</h2>

<h3>总体架构</h3>

<ul>
<li>指标采集子系统。负责信息采集、过滤、汇总和存储</li>
<li>数据处理子系统。负责数据分析、展现、预警、告警动作触发和告警</li>
</ul>


<p><img src="/images/blog/2019-06-08_3.png" title="&lsquo;image&rsquo;" ></p>

<h3>指标采集子系统</h3>

<h4>数据采集</h4>

<ul>
<li>通过客户端进行数据采集。针对特定的设备和系统开发的采集器。客户端会将采集到的数据上报到监控中心节点进行汇聚存储。这种方式比较灵活，在理论上任何对象都是可以被监控的。但客户端本身需要一定的开发工作，采用侵入的方式安装客户端也存在安全和性能风险</li>
<li>通过标准协议进行数据采集。优点是数据规范统一、普适性更广，所有Java应用都可以实现 JMX 协议，从而完成应用指标监控。但目前数据中心内的软硬件各异，设备和软件对协议的支持也不完善，在很大程度上限制了通过协议进行数据采集的能力</li>
</ul>


<h4>数据传输和过滤</h4>

<ul>
<li>HTTP、Socket连接进行点对点传输。如果 Agent（采集器）是一个 HTTP 服务端，那么采集中心节点会周期性调用 Agent 的接口获取数据，这种方式被称为 Pull（拉取）；相反，如果 Agent是一个 HTTP客户端，它就会调用中心节点的 HTTP服务，将数据发送到中心节点，这种方式被称为Push（推送）</li>
<li>RabbitMQ、Kafka等消息中间件进行传输。使用消息中间件传输的方式在结构上充分解耦。消息中间件不仅可以将监控数据进行一定程度的持久化，而且可以对监控数据进行整流，避免突发流量冲击数据收集组件。但会造成整个系统对中间件的强依赖，如果中间件发生故障，则可能导致整个系统发生故障，并且消息中间件在安装部署及后期的运维中都比较复杂。</li>
</ul>


<p>HTTP 传输的方式虽然有一定的耦合度，但系统可以足够简单，易于扩展和维护，大部分开源监控系统都采用这种方式</p>

<p>在持久化之前，通常还需要对收集的数据进行简单去重和过滤，主要有以下几个场景：</p>

<ol>
<li>客户端重复上报数据。网络不稳定等因素</li>
<li>去除无用指标。客户端上报的指标通常是全集，而有些指标是系统并不关注的</li>
</ol>


<h4>数据存储</h4>

<p>对监控数据的存储通常借助于时序数据库。监控数据的最大特点是有时间属性，每个监控数据都有一个时间维度（属性），被称为时序数据，展现了一个物体的某些特征在一段时间内的变化情况</p>

<p>时序数据的特点：</p>

<ul>
<li>一次性写入多次读取。时序数据通常不会修改更新，一旦存入，则后期只会对数据进行查询操作</li>
<li>数据流持续平稳且量大。时序数据量往往和采集点的数据量成正比，不会出现业务系统流量突增的情况，并且数据流是持续稳定的，通常间隔一个稳定的采集周期来获取数据。但存储的数据量会慢慢变得非常大。</li>
<li>查询方式以时间为维度，数据查询通常会指定一段时间，并且热数据通常都是最新采集的数据。在实际场景中通常查询最近几个小时的监控数据</li>
</ul>


<p>所以，与关系型数据库采用B+树不同，时序数据库通常采用LSM树，提供更好的写性能。因为LSM树存储框架实现的思路较简单，其先在内存中保存数据，再定时刷到磁盘，实现顺序IO操作，通过定期合并文件减少数据冗余；文件有序，保证读取操作相对快速。适用于写多、读相对少(或较多读取最新写入的数据，该部分数据存在内存中，不需要磁盘IO操作)的业务场景</p>

<h3>数据处理子系统</h3>

<h4>数据查询</h4>

<p>通过展现层（浏览器或者客户端）多维度展现实时数据和历史数据。实时数据查询严格要求数据的准确性和实时性，查询接口需要迅速响应</p>

<p>另外，可以通过一些Web工具，把后端分析的数据以可视化的方式展现。比如：</p>

<ul>
<li>折线图能够很好地展现数据变化的趋势</li>
<li>饼状图能更好地展现系统中各个子模块的占比</li>
<li>柱状图的优势在于对数据的对比</li>
</ul>


<h4>数据分析</h4>

<ul>
<li>性能分析：分析某个应用在特定时间段内的资源消耗情况，可以将应用按照资源消耗的特点分类。当应用程序出现性能问题时，要能够通过性能分析确定故障点，并在故障发生后回溯故障发生的原因</li>
<li>关联分析：每个监控数据都存在多个维度，通过关联分析可以找出数据之间的关联。在 APM 监控中，通过相同 TraceId 的关联，可以构建出程序的调用链信息，这样不仅可以分析出组件之间的调用关系，还可以分析出每个调用的耗时，从而优化系统性能</li>
<li>趋势分析：对采集数据指标进行分析，再结合平均数算法、指数平滑算法、Holt线性趋势算法或其他机器学习模型如自回归积分滑动平均，分析时序数据内在的周期性，得出未来的变化趋势。可以用于实时资源调度、预分配资源，还可以辅助数据中心的硬件采购</li>
</ul>


<h4>基于规则告警</h4>

<p>利用已经采集的监控数据，匹配用户自定义的多维度告警规则，如果满足，则触发相应规则的告警。例如，性能告警规则一般是设定某个阈值、触发次数和告警行为</p>

<p>还有一些告警属于异常告警，例如服务器宕机、网络不通等情况，通常不需要设定告警规则。如果某个告警规则被触发，则在接下来的一段时间内，为避免相同的告警被多次触发，需要进行告警抑制</p>

<p>对告警的处理，会按照用户预先设定的告警动作执行对应的操作，通常会发送短信、邮件或者触发用户定义的 webhook。高级的告警处理可以执行告警的恢复脚本，先在系统中预先设定一些常用的恢复脚本，系统能够根据发生的不同事件，判断并使用不同的脚本处理告警事故</p>
]]></content>
  </entry>
  
</feed>
